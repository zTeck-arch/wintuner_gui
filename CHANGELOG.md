# Changelog

## 0.18.0 – Was ab Werk geschützt ist, und was nebenher schon gebaut wird

**Aus einer Durchsicht der Architektur (31.08.2026)**

- **Ein hängender Intune-Aufruf friert das Fenster nicht mehr ein.** Von 22 Stellen, an denen die Anwendung mit Microsoft Graph spricht, hatten fünfzehn **keine** Zeitbegrenzung — und PowerShell wartet ohne eine solche Angabe unbegrenzt. Antwortete Intune nicht, stand das Fenster still, ohne Abbruchmöglichkeit; es blieb nur, die Anwendung zu beenden. Jetzt hat jeder dieser Aufrufe eine Obergrenze (100 s), und ein laufender Stapel lässt sich während einer Wartepause weiterhin stoppen.
- **Eine gedrosselte Zuweisung wird wiederholt, statt halb angewendet stehenzubleiben.** Beim Update einer zugewiesenen App passieren zwei Schreibvorgänge: die neue Version bekommt die Zuweisungen, danach wird die alte geleert. Scheiterte der zweite, waren **beide** Versionen zugewiesen und jemand musste das im Intune-Portal von Hand aufräumen. Die wahrscheinlichste Ursache dafür ist genau die Drosselung, die zwei schnelle Schreibvorgänge auslösen (HTTP 429) — und dagegen gab es an dieser Stelle bisher keinen zweiten Versuch. Jetzt wird gewartet, so lange Intune es vorgibt (höchstens 45 s), und erneut geschrieben. Wiederholt wird nur, was gefahrlos wiederholbar ist: das Anlegen einer neuen App bleibt ein einmaliger Versuch, damit nie eine zweite App entsteht.
- **Das Fenster lässt sich flüssig in der Größe ändern.** Beim Ziehen am Fensterrand rechnete die Anwendung die Anordnung **aller** Bereiche neu, auch der gerade unsichtbaren — zehn davon umsonst, und das dutzende Male je Sekunde. Gemessen: 15–19 ms je Ereignis vorher, 1,1–6,0 ms jetzt. Dabei kam heraus, dass sich zwei Bereiche („Einstellungen", „Eigene Installer") beim Ziehen bisher **gar nicht** neu angeordnet haben; das tun sie jetzt.
- **Der Start ist etwas schneller.** Eine einzelne Systemabfrage für den Namen des Betriebssystems im Protokollkopf kostete gemessen 400 ms bei jedem Start. Im Protokoll steht jetzt die Windows-Build-Nummer, die einen Supportfall ohnehin genauer eingrenzt.
- **Die Update-Suche schreibt ihren Versionsspeicher einmal statt bei jedem Paket.** Bei 100 geprüften Apps war das vorher 100 Mal dieselbe Datei komplett neu geschrieben, mitten zwischen den Netzabfragen.
- **Ein unsinniger Wert in der `settings.json` kippt nicht mehr das Menü.** `MaxRecentLogins` gilt nur noch zwischen 1 und 50; alles darüber fällt auf 15 zurück. `KeepVersionCount` bleibt bewusst ohne Obergrenze — dort würde ein Rückfall auf den Standardwert Versionen **löschen**, die jemand behalten wollte.

- **Geschützte Apps lassen sich jetzt auslassen, statt den ganzen Lauf abzubrechen.** Die Rückfrage vor einem Lauf war eine Ja-oder-Nein-Frage: wer zehn Apps angehakt hatte und dabei zwei geschützte übersah, musste abbrechen, die zwei suchen, abwählen und von vorn anfangen. Genau so gewöhnt man sich an, eine Rückfrage wegzuklicken. Es gibt jetzt drei Wege: „Ohne die geschützten fortfahren" (der Vorgabeknopf), „Alle aktualisieren, auch geschützte" und Abbrechen. Beim mittleren Weg fallen nur die geschützten Zeilen heraus, der Rest läuft unverändert durch, und die Statuszeile nennt beide Zahlen. Waren ausschließlich geschützte Apps angehakt, sagt die Meldung genau das — statt „abgebrochen", was nach einem Fehler des Benutzers aussähe. Ins Protokoll geht weiterhin, **welche** Apps freigegeben oder ausgelassen wurden, nicht nur wie viele. Und wie bisher lässt sich diese Frage über „Rückfragen abschalten" nicht wegdrücken; ein weggeklickter Dialog bedeutet immer Abbruch, nie „alle aktualisieren".
- **TeamViewer, Jamf und Splashtop sind ab Werk geschützt.** Die Schutzliste aus 0.17.0 startete leer — in einer neu eingerichteten Umgebung war also nichts geschützt, bis jemand daran dachte. Genau dort passiert der Unfall: ein Lauf löst die selbst gebaute App ab, zieht ihre Zuweisungen mit, und bei einem von Hand gebauten Paket holt das kein zweiter Lauf zurück. Die drei Muster gelten für **jede** Fassung des Produkts („TeamViewer Host", „Jamf Connect", „Splashtop Streamer"). Geschützt heißt weiterhin nicht gesperrt: die App bleibt sichtbar und anhakbar, ein Lauf fragt bei ihr nur ausdrücklich nach. Die Muster kommen auch bei einer **bestehenden** Installation an, ohne Ihre eigenen Einträge anzurühren — und ein Muster, das Sie entfernen, kommt **nicht** beim nächsten Start zurück.
- **Geschützte Apps stehen jetzt in Rot, nicht mehr im selben Orange wie drei Randbemerkungen.** „Diese App wird selbst paketiert" ist die stärkste Aussage der Liste und sah aus wie „bei dieser Zeile ist die Zuweisung unklar". Außerdem behält eine geschützte Zeile ihre Farbe jetzt auch dann, wenn zusätzlich etwas anderes an ihr auffällt — vorher überschrieb die nächste Regel das Rot, also genau dann, wenn es am wichtigsten war.
- **Warnzeilen sind in den hellen Designs endlich lesbar.** Gemessen erreichte das bisherige Orange auf hellem Grund 2,33 : 1 — in sechs der sieben Designs war die Warnung kaum zu erkennen. Die Farben richten sich jetzt nach dem Design (gemessen 4,4 bis 6,7 : 1). „Nicht prüfbar" ist dabei bewusst neutral-grau statt rötlich: eine Lücke ist keine Gefahr, und Rot gehört den geschützten Apps.
- **Neuer Bereich „Kundendaten".** Alles, was sich die Anwendung über Ihre Kunden merkt, stand an drei Stellen: Kundennamen unter Extras, gemerkte Anmeldungen im Kopf und noch einmal unter Extras — und die Gruppen-Favoriten nur hinter dem Knopf „Gruppen…" neben einem Zuweisungsziel, wo sie ohne Vorwissen niemand fand. Jetzt an einem Ort, mit Anzahl und Inhalt, in der Gruppe „Dieser Rechner": es sind rein lokale Daten, nichts davon wird nach Intune geschrieben. Gemerkte Anmeldungen lassen sich dort erstmals **einzeln** entfernen, nicht nur alle auf einmal. In den Protokollen und in der Statuszeile stehen weiterhin nur Anzahlen — ein Protokoll gibt man aus der Hand.
- **Die Liste der erkannten Apps nutzt das Fenster.** Sie stand auf vier Zeilen, egal wie groß das Fenster war; darunter blieben 167 px leer, während man durch dreißig Einträge scrollte. Jetzt bekommt sie den Platz, der da ist.
- **Die erweiterten Optionen im WinGet-Bereich überlappen nicht mehr.** Aufgeklappt lag „Installationsstichtag planen (sonst so bald wie möglich)" 98 px über seinem eigenen Datumsfeld, „Verfügbarkeit planen…" 64 px und „Neustart-Countdown vorher anzeigen (Minuten)" 62 px über seinem Zahlenfeld. Die Spalte richtet sich jetzt nach der breitesten Beschriftung der jeweiligen Sprache. Aufgefallen ist das erst, als die Layout-Prüfung den Bereich zum ersten Mal **aufgeklappt** vermessen hat.

**Aus dem Betrieb (02.09.2026)**

- **Das nächste Paket wird gebaut, während das aktuelle hochgeladen wird.** Bisher lief je App streng nacheinander: Paket bauen, dann hochladen, dann die nächste App. Gemessen an einem echten Lauf (8 Vorgänge, ~8,5 Minuten) steht Paketieren gegen Hochladen bei Logi Tune 256 s zu 32 s, bei Chrome 13 s zu 25 s — nacheinander addiert sich beides. Jetzt entsteht das Paket der nächsten App in einem zweiten Hintergrundprozess, während die aktuelle noch hochlädt; gespart wird je App grob die kürzere der beiden Phasen. Hochgeladen wird weiterhin **eine App nach der anderen** (Intune drosselt, und die Sitzung gilt je Prozess) — nebenläufig ist nur das Paketieren, und das schreibt ausschließlich lokale Dateien. Übernommen wird ein vorab gebautes Paket **nur** bei Zeichen-für-Zeichen identischem Argumentsatz; passt etwas nicht, wird es verworfen und wie bisher gebaut. Ein Abbruch, ein Trennen oder das Schließen des Fensters stoppen auch den Vorab-Bau.
- **Die Werksliste der geschützten Apps deckt jetzt Fernwartung, RMM und Passwortmanager ab.** Bisher standen dort drei Namen, weil nur diese drei ausdrücklich genannt worden waren. Dazugekommen sind AnyDesk, ScreenConnect/ConnectWise, N-able/N-central, Datto sowie Keeper, 1Password, Bitwarden, LastPass und KeePass. Der Grund ist bei beiden Gruppen derselbe: Der Installer trägt etwas in sich, das ein aus WinGet gebautes Paket nicht hat — bei Fernwartung und RMM die Zuordnung zum Betreuer (Mandanten-Id, Kundenschlüssel, eigens erzeugtes Installationspaket), bei Passwortmanagern die Richtliniendatei oder die SSO-Anbindung. Ein Ersatz durch die nackte Herstellerfassung installiert dasselbe Produkt „leer": Der Rechner meldet sich bei niemandem mehr, und ausgerechnet der Zugang, über den man das reparieren könnte, ist weg. Geschützt heißt wie bisher nicht gesperrt — die App bleibt sichtbar und anhakbar, ein Lauf fragt bei ihr nur ausdrücklich nach. Die Nachträge kommen auch bei **bestehenden** Installationen an, und ein Muster, das Sie entfernen, kommt nicht zurück.
- **Eine Hash-Abweichung überspringt die App nicht mehr, sondern liefert die zuletzt angebotene Version.** Tauscht ein Hersteller den Installer unter derselben Adresse aus, kennt das WinGet-Manifest noch die alte Prüfsumme, und das Paketieren scheitert mit „Hash mismatch" — bei Google Chrome am 02.09.2026 genau so. Bisher hieß das im Stapellauf: App übersprungen, alter Stand bleibt stehen, und niemand merkt es, bis jemand das Protokoll liest. Das dauert regelmäßig Tage, bis das Manifest nachzieht — über mehrere Apps hinweg entsteht daraus genau die Lücke, die dieses Werkzeug schließen soll. Jetzt wird die **nächstältere angebotene** Version gebaut und ausgerollt, aber nur, wenn sie gegenüber dem, was im Tenant liegt, überhaupt noch ein Update ist. Ist sie das nicht — etwa weil sie beim letzten Lauf bereits so ausgerollt wurde —, bleibt es beim Fehlschlag, statt dasselbe Paket noch einmal zu bauen. Das Protokoll nennt in beiden Fällen den Grund und **welche** Version statt der neuesten geliefert wurde. Der interaktive Weg fragt weiterhin nach (Wiederholen / vorherige Version / Abbrechen); neu ist nur, dass der unbeaufsichtigte Lauf nicht mehr kommentarlos aufgibt.
- **Fehlermeldungen zu Zuweisungen sprechen jetzt Deutsch.** „Keine Berechtigung zum Ändern von Zuweisungen (HTTP 403)…" stand in der Statuszeile bisher auch dann auf Englisch, wenn der Rest des Fensters Deutsch war — ausgerechnet in dem Satz, der sagt, was zu tun ist. Das Protokoll bleibt bewusst englisch: es wandert in Tickets.
- **Der Bereich „Kundendaten" passt jetzt auch auf einen kleineren Bildschirm.** Bei 1146×854 brauchte er gemessen 761 px bei 639 px Sicht und scrollte deshalb; die fehlenden 122 px steckten in der Knopfreihe unter jeder Liste. Die Knöpfe stehen jetzt rechts in der Titelzeile, wo ohnehin nur der kurze Kartentitel steht — Bedarf 627 px, kein Bildlauf mehr. Auf 1920×1080 wachsen die drei Listen dadurch von 148 auf 188 px, zeigen also rund drei Einträge mehr, ohne dass man scrollt.
- **Ein halb angewendeter Zuweisungsumzug wird wieder richtig gemeldet.** Beim Update einer zugewiesenen App bekommt zuerst die neue Version die Zuweisungen, danach wird die alte geleert. Scheiterte der zweite Schritt, meldete das Protokoll „nichts geändert" — dabei waren in Wahrheit **beide** Versionen zugewiesen. Wer dieser Meldung folgte, suchte an der falschen Stelle. Jetzt steht dort wieder „PARTIALLY applied … Both versions are assigned right now" samt der Anweisung, die alte Version im Portal zu entfernen oder den Lauf zu wiederholen.

## 0.17.0 – Was der Lauf nicht anfassen darf, und was er bisher übersehen hat

**Zehnte Rückmeldungsrunde**

- **Geschützte Apps: die Ausnahmeliste für selbst paketierte Software.** Splashtop, TeamViewer, Passwortmanager, kundeneigene Installer — ein Update darauf baut eine **neue** App, löst die vorhandene ab und zieht deren Zuweisungen mit. Bei einem von Hand gebauten Paket lässt sich das durch keinen zweiten Lauf zurückholen. Solche Apps lassen sich jetzt schützen: per Rechtsklick auf ihre Zeile in der Update-Liste, oder über „Geschützte Apps…" (Update-Ansicht und Einstellungen führen **dieselbe** Liste, gespeichert wird sofort). Sie bleiben sichtbar und anhakbar — man soll ja sehen, dass es eine neuere Version gibt —, aber ein Lauf fragt bei ihnen **ausdrücklich** nach, und diese Rückfrage lässt sich über „Rückfragen abschalten" **nicht** wegdrücken. Einträge ohne Platzhalter treffen den App-Namen genau, mit `*` oder `?` gelten sie als Muster: „Zoom Rooms" schützt eine App, „Zoom*" alle. Die Liste ist **global**, nicht pro Kunde — eine Liste pro Tenant wäre in jeder neuen Umgebung leer, und genau dort passiert der Unfall.
- **Die Update-Suche sieht jetzt auch Apps ohne WinTuner-Marke** (neue Einstellung, standardmäßig **an**). Das Modul listet nur Apps, deren Notizfeld seine eigene Marke trägt. Diese Marke sagt, **wer** die App angelegt hat — nicht, ob WinGet eine neuere Version kennt. Handgebaute Pakete und Apps, deren Marke im Betrieb entfernt wurde, waren für die Suche deshalb unsichtbar. Jetzt wird jede Win32-App des Tenants geprüft. Eine WinGet-Id wird weiterhin nur übernommen, wenn der Name **genau** trifft, ein Treffer klar dominiert, oder eine Zuordnung hinterlegt ist; alles Mehrdeutige wird übersprungen und protokolliert — eine falsche Id würde das falsche Produkt paketieren und die echte App ablösen. Zusätzlich wird eine lose im Notizfeld stehende Paket-Id gelesen (`WinTuner Google.Chrome` ohne Klammern), aber nur, wenn sie wie `Hersteller.Produkt` aussieht: Versionsnummern und Dateinamen werden abgelehnt.
- **Apps, die nicht geprüft werden konnten, stehen jetzt in der Liste — gesperrt und mit Grund.** Vorher standen sie nur im Protokoll, und das Fenster ließ den Tenant sauberer aussehen, als er war: „keine Updates gefunden" und „vier Apps konnte ich gar nicht ansehen" sind verschiedene Antworten. Im Lauf vom 28.08.2026 betraf das Keeper, Harmony SASE und den Teams Machine-Wide Installer. Gesperrte Zeilen nennen den Grund („keine WinGet-Id sicher zuzuordnen", „Intune meldet keine Version"), sind nicht anhakbar, werden von „Alle auswählen" übersprungen und von einem Lauf ausgelassen — auch wenn sie auf anderem Weg angehakt wurden. Zwei nicht prüfbare Apps werden außerdem **nicht** zu einer Zeile zusammengefasst.
- **WinGet-Id von Hand zuordnen.** Rechtsklick auf eine gesperrte Zeile → „WinGet-Id zuordnen…". Die Eingabe wird auf die Form `Hersteller.Produkt` geprüft (eine Versionsnummer, ein Dateiname oder ein Anzeigename wird abgelehnt und **nichts** gespeichert), danach läuft die Suche sofort erneut. Eine leere Eingabe entfernt die Zuordnung wieder.
- **Die Bilanz der Suche stimmt wieder.** Apps ohne WinGet-Id wurden **zweimal** abgezogen — einmal als übersprungen, einmal als fehlgeschlagene Prüfung. Im Lauf vom 28.08.2026 stand deshalb „0 aktuell + 3 Prüfung fehlgeschlagen", obwohl Chrome, WebView2 und VS Code geprüft und aktuell waren. Die Rechnung läuft jetzt an **einer** Stelle und prüft sich selbst; geht sie nicht auf, sagt das Protokoll das, statt eine falsche Zahl zu zeigen. Gesperrte Zeilen zählen nicht als Update-Ziel, und die Statuszeile meldet keine Arbeit, die es nicht gibt.
- **Gemerkte Anmeldungen: 15 statt 8.** Bei acht Plätzen fiel bei Betreuung mehrerer Kunden ständig eine Adresse hinten heraus. Die zuletzt benutzte rückt weiterhin nach oben statt sich zu verdoppeln (Groß-/Kleinschreibung zählt dabei nicht).
- **Der Leistungsnachweis zeigt beim Öffnen den aktuellen Stand.** Er wurde einmal erzeugt und blieb dann stehen; nach einem Lauf musste man ihn von Hand neu anfordern. Jetzt wird er beim Betreten des Bereichs neu erzeugt — aber nur, wenn im Feld noch der selbst erzeugte Text steht. Eine Änderung von Hand bleibt erhalten.
- **Entfernt: die einmalige Übernahme der Einstellungen aus `%APPDATA%\Verteilwerk`.** Der Umbenennungsplan ist endgültig verworfen; das Sicherheitsnetz betraf nur eine nie veröffentlichte Zwischenfassung und hätte künftig nur noch verwirrt.

## 0.16.0 – Durchsicht der gesamten Oberfläche

**Neunte Rückmeldungsrunde**

- **Die wirksamen Einstellungen stehen jetzt im Protokoll** — beim Start und nach jedem Speichern, im selben Format. Sechs Zeilen: Dateipfade und Löschfrist, Design/Sprache/Fenster, was beim Anmelden und beim Start **von selbst** passiert, die Einstellungen des Update-Laufs, der Zustand der Rückfragen (samt der Version, für die das Risiko akzeptiert wurde) — und von allen kundenbezogenen Listen (Anmeldeadressen, Kundennamen, Gruppen-Favoriten, Paket-Übersteuerungen) nur die **Anzahl**. Ein Protokoll wird weitergegeben; Pfade erklären Fehler, Kundendaten nicht.
- **Weniger Schritte beim Anmelden.** Das Dashboard stellte drei Tenant-Abfragen, und bei einem Tenant ohne WinTuner-Apps hängte jede eine Gegenprobe und einen langen Erklärungsabsatz an — neun Anfragen und drei Absätze für dreimal die Zahl 0. Jetzt: (1) ohne verwaltete App wird die Update-Kennzeichen-Abfrage übersprungen (ohne Apps kann es kein Update geben), (2) die paginierte Graph-Gegenprobe liest die App-Liste **einmal** und teilt sie lokal in aktiv/abgelöst statt zweimal durchzulaufen, (3) die ausführliche Erklärung zum leeren Inventar steht einmal pro Sitzung, danach in einer Zeile.

**Achte Rückmeldungsrunde**

- **Der untere Bereich bleibt jetzt gleich hoch — immer.** Der Protokollkasten wuchs während eines Laufs von 120 auf 210 px und schrumpfte danach zurück; zusätzlich legte ein Lauf eine eigene Fortschrittszeile an (32 px). Der Inhalt darüber verlor also mitten in der Arbeit 122 px, bekam überall Bildlaufleisten, und danach sprang alles zurück. Der Kasten hat jetzt eine feste Höhe, und Fortschrittstext samt „Lauf stoppen" teilen die Zeile des Protokoll-Umschalters. Gemessen am gebauten Skript in drei Fenstergrößen: unterer Bereich 191 px und Inhaltshöhe **unverändert** vor, während und nach einem Lauf; in allen sieben Designs und beiden Sprachen keine Überlappung.
- **Der Knopf „Jetzt aktualisieren" im Dashboard ist wieder weg.** Der Stand („Stand 13:41 …") bleibt — geladen wird bei der Anmeldung und nach jedem Eingriff, der Knopf hat nur die Zeile verbreitert.
- **Zuweisungen fragen immer nach.** Wer in „Alle Tenant-Apps" die Zuweisung einer App schreibt, ändert, **wer** die App bekommt. Diese Rückfrage lässt sich jetzt nicht mehr über „Rückfragen abschalten" wegdrücken (`Confirm-ChangeAction -AlwaysAsk`); die Einstellung nimmt weiterhin die Rückfragen der eigenen Routine ab (Update-Lauf starten, Inhalt ersetzen).
- **Drei Zeitfresser aus dem Protokoll beseitigt.** (1) Die Installationssonde fragte in diesem Tenant erst zwei Quellen, die mit HTTP 400 antworten, und erst dann die dritte — 5–8 s pro Sonde, mehrfach je Lauf. Die Quelle, die geantwortet hat, wird jetzt zuerst gefragt (dieselben Quellen, dieselbe Sicherheitsregel: eine Null braucht weiterhin eine zweite Quelle). (2) „Welche Version ist die neueste?" wurde beim Anmelden zweimal je App gestellt — erst von der Dashboard-Kachel, Sekunden später von der automatischen Update-Suche. Jetzt ein Kurzzeit-Zwischenspeicher (5 Minuten); wer selbst auf „Nach Updates suchen" klickt, liest garantiert frisch. (3) Der **automatische** Favoritenlauf wartete 35 s HTTP-429-Sperre ab, während er die Busy-Sperre hielt — Anmeldung und Update-Suche warteten mit. Unbeaufsichtigt wird eine Sperre jetzt einmal abgewartet (5 s) statt dreimal (50 s).

**Siebte Rückmeldungsrunde**

- **„Keine Apps in Intune gefunden", während im Portal Dutzende Apps stehen.** Zwei verschiedene Ursachen, die dieselbe Meldung hatten. Erstens der Wettlauf (siehe unten) — dagegen liest die Abfrage jetzt gegen und holt eine **zweite Meinung direkt von Graph** (paginiert, gleicher Filter). Zweitens der häufigere Fall: diese Suche kann nur Apps mit der `[WinTuner|`-Marke im Notizfeld vergleichen, weil nur die eine WinGet-Paket-Id tragen. Handgebaute Apps (Skripte, Treiber, Store-Apps) waren nie gemeint und stehen unter „Alle Tenant-Apps". Die Meldung sagt das jetzt — und wenn abgelöste Versionen vorhanden sind, nennt sie den wahrscheinlichen Grund (die neueren Versionen wurden gelöscht).
- **Titel, Menüleiste und Navigation stehen in einer Flucht (30 px).** Gemessen wurde diesmal an den **gezeichneten Pixeln** einer Bildschirmkopie des laufenden Fensters, nicht an Koordinaten: Menüleiste 20, Titel 30, Nav-Symbol 30, Nav-Text 48. Die Menüleiste rückt ihren ersten Eintrag um 14 px aus sich heraus ein — der Rest kommt jetzt als Innenabstand dazu. Der Nav-Text bleibt hinter seinem Symbol, wie in Windows-11-Apps.
- **Die Kopfzeile ist 14 px flacher (78 → 64).** Für eine 36 px hohe Knopfreihe waren 22 px oben und 20 px unten zu viel; der Platz fehlte unten in den Listen. Kopfzeilenhöhe und Inhaltsbeginn hängen jetzt an einer Rechnung, sonst bleibt ein leerer Streifen oder der Inhalt klebt an der Akzentlinie.
- **Die Liste „Abgelöste (alte) App-Versionen" zeigt, was sie hat.** Bei sechs gefundenen Apps waren zwei Zeilen sichtbar: die Karte richtet sich nach der Zeilenzahl, aber nach dem Füllen der Liste hat niemand die Anordnung neu berechnet. Jetzt schon — und mit Inhalt sind es mindestens **fünf** Zeilen (bis zehn), leer bleibt sie klein, damit die Update-Liste darüber den Platz bekommt. Gemessen bei 1146×854, 1366×768 und 1920×1080: aus 2 wurden 6 sichtbare Zeilen.
- **Das Fenster richtet sich beim ersten Start nach dem Bildschirm** — 80 % der Arbeitsfläche, nach unten begrenzt durch die Entwurfsgröße (1060×850, darunter wird die Oberfläche enger, nicht besser) und nach oben durch die Arbeitsfläche minus 8 px. Eine gespeicherte Größe gewinnt weiterhin immer. Vorher startete es überall mit 1060×850: auf einem 4K-Schirm eine Briefmarke, auf einem 1366×768-Notebook zu hoch.
- **Neue Einstellung: optionale Leseberechtigungen direkt bei der Anmeldung.** Holt Group.Read.All und DeviceManagementManagedDevices.Read.All in **einem** zusätzlichen Anmeldevorgang; danach zeigen Zuweisungen sofort Gruppennamen und die erkannten Apps laden ohne Nachfrage. Standardmäßig aus (es kostet ein zweites Anmeldefenster), und ein Fehlschlag bleibt folgenlos: er steht im Protokoll, alles andere arbeitet weiter, und die Berechtigung wird bei Bedarf erneut angefordert.

**Sechste Rückmeldungsrunde**

- **„Trennen" während eines Laufs trennte sofort — und der Lauf lief weiter.** Gemeldet aus einem echten Lauf: nach einem Fehler bei Google Chrome wurde getrennt, danach paketierte, wartete und lud die Anwendung noch drei Apps hoch, gegen eine Sitzung, die es nicht mehr gab. „Trennen" und „Abmelden" stoppen jetzt **zuerst den Lauf** und werden bis zu dessen Ende zurückgestellt; zusätzlich bricht der Stapellauf am nächsten App-Wechsel von selbst ab, wenn keine Sitzung mehr besteht. Ein laufender Upload nach Intune wird nie unterbrochen — sonst bliebe in Intune eine halbe App zurück.
- **Der Abbruch-Knopf ist zurück.** Er steht rechts in der Fortschrittszeile und heißt „Lauf stoppen". Entfernt wurde er, weil er neben einem Balken saß, der während der langen Schritte einfror; seither läuft der Paketbau in einem eigenen Runspace und die Wartepausen pumpen die Nachrichtenschleife — der Klick kommt also an. Wirkung: laufender Paketbau und Wiederholungspause brechen **sofort** ab, ein laufender Upload wird fertig gemacht, danach endet der Lauf. Gezeigt wird der Knopf nur bei Vorgängen, die den Abbruch auch abfragen (Update-Suche und Update-Lauf); ein Test verhindert, dass ein Vorgang ihn anbietet, ohne ihn zu prüfen.
- **Titel und Seitenleiste haben denselben linken Abstand.** Der Titel „WinTuner GUI" saß auf 21 px, die Navigationseinträge auf 30 px — zwei getrennte Pixelwerte aus zwei Baustellen. Der Einzug kommt jetzt aus **einer** Rechnung (`$script:navContentIndent`), nachgemessen am gebauten Skript: beide auf 30 px.

- **„Keine Apps in Intune gefunden" bei einem Kunden mit 11 Apps.** Der bekannte Modul-Wettlauf („Collection was modified") schlägt nicht immer als Ausnahme durch — manchmal kommt einfach eine **leere Liste** zurück. Damit sah der Wiederhol-Mechanismus nichts, was er wiederholen könnte, die Null wanderte in den Cache, und die Update-Suche meldete auf dieser Grundlage einen leeren Tenant (`managed=0 updates=0 superseded=6`). Drei Schlösser dagegen: eine leere Antwort wird **einmal gegengelesen**, bevor sie gilt; eine leere Antwort wird **nicht über ein gefülltes Inventar desselben Kunden** gecacht; und 0 aktive Apps bei vorhandenen abgelösten Versionen ist ein **Widerspruch** (eine App ist nur abgelöst, weil eine neuere sie ablöst) — dann zeigen die Kacheln „—" und die Suche sagt „Laden fehlgeschlagen", statt eine Null zu behaupten.
- **„Laden der Apps aus Intune fehlgeschlagen", weil zwei Abfragen kollidierten.** Der Hintergrund-Runspace fährt genau **eine** Pipeline; weil die Inventar-Abfrage die Nachrichtenschleife weiterlaufen lässt (das ist ihr Zweck), konnte ein Klick auf „Dashboard" eine zweite starten — Antwort: „The pipeline was not run because a pipeline is already running." Ist der Runspace besetzt, wird die Abfrage jetzt einfach inline erledigt statt zu scheitern.
- **Der Leistungsnachweis verlor drei erledigte Updates.** Nach dem Trennen mitten im Lauf war `$script:currentUserUpn` leer, und alle danach erfassten Einträge bekamen einen **leeren Tenant** — der Nachweis filtert nach Tenant, also waren sie beim nächsten Start unter „Letzte Sitzung" nicht mehr auffindbar (in der Datei nachgesehen: drei Einträge, Tenant `""`). Erfasst wird jetzt unter der zuletzt angemeldeten Adresse, die das Trennen überlebt. Zusätzlich vergleicht der Nachweis die **Domäne** statt der ganzen Adresse: wer sich mit dem zweiten Admin-Konto desselben Kunden anmeldet, sah seinen eigenen Nachweis vorher nicht. Alte Einträge ohne Zuordnung werden getrennt angehängt und ausdrücklich als „bitte prüfen, ob sie zu diesem Kunden gehören" ausgewiesen — nicht stillschweigend diesem Kunden zugeschlagen, der Text landet in einem Ticket.
- **Das Dashboard fragt den Tenant nicht mehr bei jedem Besuch ab.** Jeder Klick auf „Dashboard" kostete drei Abfragen (Inventar, Update-Kennzeichen, abgelöste Apps) auf dem UI-Thread — das war das kurze Hängen beim Wechseln. Aktualisiert wird jetzt bei der Anmeldung, **nach jedem Eingriff** (der Merker sitzt in `Add-SessionActivity`, der einen Stelle, die jeder schreibende Weg durchläuft) und auf Knopfdruck. Unter den Kacheln steht der **Stand**, damit die Zahlen nie stumm veralten — genau der Fall „8 veraltete Apps stimmt nicht mehr".
- **Die Kachel heißt jetzt „Abgelöste Versionen".** Sie zählt alte, von einer neueren ersetzten Versionen — eine Zahl, die mit jedem Update **wächst**. Als „Veraltete Apps" direkt neben „Verfügbare Updates" gelesen, sah sie wie eine zweite, widersprüchliche Update-Zahl aus. Der Tooltip sagt jetzt, was sie zählt und was nicht.
- **Zuweisungen zeigen den Namen der Gruppe, nicht ihre GUID.** In Intune steht dort der Klarname; hier stand `Group 1f4c…`. Aufgelöst wird in drei Stufen: Gruppen-Favorit dieses Kunden (kostenlos), Graph mit dem Token der Anwendung, und — falls die optionale Gruppen-Berechtigung in dieser Sitzung schon erteilt wurde — über die Graph-Sitzung. Von dort wird **nie** ein Zustimmungsdialog geöffnet (die Funktion läuft in einer Schleife über eine Liste); fehlt die Berechtigung, wird nach dem ersten 403 abgeschaltet und einmal ins Protokoll geschrieben, wie man sie erteilt. Namen und Fehlschläge werden je Sitzung gemerkt und beim Tenant-Wechsel verworfen.
- **Die Spalte „Zuweisung" in den App-Einstellungen füllt sich selbst.** Sie stand auf „(zum Laden anklicken)" — die wichtigste Auskunft der Liste gab es erst nach einem Klick je Zeile. Jetzt wird sie nach dem Laden der Liste durchgelesen, eine App nach der anderen, mit Fortschritt in der Statuszeile, ohne Busy-Sperre und abbrechbar. Jede App wird **einmal** gelesen: gemerkt wird am Beutel (Graph-Id), sonst hätte jeder Tastendruck im Filterfeld alles neu gelesen.
- **Reihenfolge der Seitenleiste ist jetzt eine Entscheidung.** Innerhalb einer Gruppe entschied die Reihenfolge der Quelldateien — „Alle Tenant-Apps" (Teil 82) stand deshalb vor „Erkannte Apps" (Teil 85). Unter „Bestand" steht jetzt: Updates, Erkannte Apps, Alle Tenant-Apps, App-Einstellungen. Gemessen am gebauten Skript.
- **„Vorgang läuft…" sagt nichts mehr über fehlende Stückzahlen.** Der Zusatz „(für diesen Schritt gibt es keine Stückzahl)" beantwortete eine Frage, die niemand gestellt hat.

**Fünfte Rückmeldungsrunde**

- **Das Fenster startete unter der Taskleiste.** Die Größe war auf die Arbeitsfläche begrenzt, die Lage aber über `CenterScreen` bestimmt — und das zentriert im **gesamten** Bildschirm, Taskleiste eingerechnet. Bei einer gespeicherten Größe wurde überhaupt keine Lage gesetzt. Jetzt wird in der **Arbeitsfläche** zentriert (die schließt die Taskleiste aus, an welcher Kante sie auch klebt), mit 8 px Luft je Achse, und das Protokoll nennt die gewählte Lage.
- **Der Name ist wieder WinTuner GUI** — siehe „Name bleibt" weiter unten.
- **„App-Zuweisungseinstellungen" ist aus dem Extras-Menü verschwunden.** Der Bereich in der Seitenleiste und der Knopf in „Alle Tenant-Apps" sind die zwei Wege, die es braucht; ein dritter im Menü machte die Leiste unglaubwürdig.
- **Der Leistungsnachweis ist ein eigener Bereich** in der Seitenleiste (Gruppe „Dieser Rechner") statt eines Menüeintrags. Dieselbe Funktion, nur nicht mehr versteckt: Sprache und Sitzung wählen, Text lesen, kopieren. Der Fensterweg bleibt bestehen.
- **Der 403 bei den erkannten Apps bietet jetzt die Berechtigung an.** Vorher stand nur da, welche fehlt. Jetzt öffnet sich derselbe Dialog wie bei der Gruppensuche: mit dem angemeldeten Konto erteilen, **mit einem anderen Konto anmelden** oder abbrechen. Wird sie erteilt, läuft die Abfrage sofort erneut — über die Graph-Sitzung, die die Berechtigung trägt. Der Abruf liegt jetzt an **einer** Stelle (`Get-TenantDetectedApps`) und kann beide Wege.
- **Die App-Zuweisungseinstellungen haben eine Suchmaske.** Filtern nach Name oder Version, während die Auswahl erhalten bleibt: gemerkt wird die Graph-Id, nicht die Zeile. „Alle auswählen" wirkt bewusst nur auf die **sichtbaren** Zeilen — bei aktivem Filter wäre alles andere eine Falle. Angewandt wird auf alle ausgewählten Apps, auch auf die, die der Filter gerade ausblendet; die Statuszeile nennt beide Zahlen. Von der eigenen Messung gefunden: die erste Fassung verlor beim Filtern die Haken.
- **Die erweiterten Zuweisungsoptionen im Store-Bereich sind neu angeordnet.** Sie standen als eine lange Spalte untereinander, mit Kästchen, die aus der Reihe fielen, und einem Datumsfeld am rechten Rand. Jetzt gemessen angeordnet — Beschriftungsspalte hinter der breitesten Beschriftung, ganze Aussagen (Kästchen) am linken Rand, Neustart-Unterwerte eingerückt, Karten-Höhe aus dem Inhalt statt aus zwei Konstanten. Ab genügend Breite zweispaltig.
- **Die Gruppen-Favoriten sind auffindbar.** Der Knopf dafür war nur eingeblendet, wenn im Auswahlfeld „Bestimmte Gruppe" stand — die einzige Stelle, an der man Gruppen-IDs eines Kunden pflegt, war damit praktisch versteckt. Er heißt jetzt „Gruppen…", steht **immer** neben „Zuweisen an" (WinGet, Store, Erkannte Apps) und erklärt sich im Tooltip.
- **Artefakte entfernt.** Die Einstellung `RepoMoveNoticeShown` und der Merker `settingsFileExisted` gehörten zum entfallenen Umzugshinweis; drei UI-Texte (`MenuAppSettings`, `TtAppSettingsMenu`, `MenuShowLeistung`) und ein durch seine Detail-Fassung ersetzter (`PackageCreationFailedStatus`) waren tot. Der Tooltip der Favoriten-Knöpfe war als Text vorhanden, aber an keinen Knopf gehängt — jetzt schon.

**Aus der Nachprüfung dieser Runde**

- **Nach dem Löschen abgelöster Apps galt die Anwendung dauerhaft als „beschäftigt".** Der Handler zeigte die Fortschrittsanzeige, blendete sie aber nie aus — kein `finally`. Und weil die Sichtbarkeit dieser Anzeige die **Sperre gegen gleichzeitige Vorgänge ist**, wurde danach jeder weitere Klick abgewiesen oder in die Warteschlange gelegt, bis irgendein anderer Vorgang die Anzeige zufällig ausblendete. Der Fehler stammt aus der Balken-Fassung (dort blieb der Balken auf 100 % stehen) und ist erst durch eine **neue StaticCheck-Regel** aufgefallen: jeder `Show-Progress` braucht ein `Hide-Progress` im selben Handler, Ausnahmen nur mit ausdrücklichem Marker im Kommentar. Die erste Fassung dieser Regel war selbst falsch (sie suchte bis zum Skript-Rumpf hoch und war deshalb immer grün) — die zweite findet den Fehler.
- **Ein Designwechsel zog die Anordnung von fünf Bereichen nicht nach.** Nur Einstellungen und „Eigene Installer" wurden neu angeordnet; Updates, Alle Tenant-Apps, Store, Lokale Pakete und App-Einstellungen behielten die Geometrie der vorherigen Schriftart, bis man sie einmal verließ und neu öffnete. Jetzt laufen alle mit.
- **Vor der Anmeldung sagten zwei leere Listen das Falsche.** „Auf 'Nach Updates suchen' klicken" bzw. „auf 'Erkannte Apps scannen' klicken" — genau das geht ohne Anmeldung nicht. Der Verbindungszustand wurde nur bei An- und Abmeldung in die Leerzustände geschrieben, nie beim Start; jetzt schon.
- **Die Store-Liste sagt, was sie ist.** Ihre erste Spalte heißt jetzt „Bereits im Tenant (Übersicht)", und das Info-Symbol des Bereichs erklärt, dass Markieren nichts bewirkt und Zuweisungen bestehender Apps unter „Alle Tenant-Apps" geändert werden. Genau diese Verwechslung war in der Rückmeldung.
- **Die optionale Gruppen-Berechtigung fragte nach jedem Klick erneut,** wenn sie mit einem *anderen* Konto erteilt wurde: die Prüfung verlangte, dass die Graph-Sitzung auf demselben Konto läuft wie die Tenant-Verbindung — beim Weg „anderes Konto" nie der Fall. Ein fehlgeschlagener Versuch sperrte außerdem die ganze Sitzung; jetzt sperrt nur ein ausdrückliches Abbrechen.
- **Die Fortschrittsanzeige erreicht am Ende wirklich 100 %.** Sie blendete bei (n-1)/n aus — bei elf Apps also bei 91 %, während der Lauf durch war.
- **Gedämpfte Beschriftungen merken sich am Steuerelement selbst,** nicht an dessen Hashcode: ein Hashcode wird nach dem Verwerfen wiederverwendet, und dann wäre irgendwann eine fremde Beschriftung gedämpft. Der modale Editor räumt seine Einträge beim Schließen weg.
- **Zwei neue Messungen in `tests/LayoutProbe.ps1`:** Kontrast **jeder** Beschriftung in **allen sieben** Designs, und abgeschnittener Text (Knöpfe und Kästchen einzeilig, umbrechende Beschriftungen über die Höhe). Beide gegengeprüft — mit künstlich verschärfter Schwelle melden sie 49 Stellen, mit der echten null. Die Prüfung geht jetzt jeden Bereich in jedem Design durch, ohne ihn neu zu öffnen: so fällt auch auf, wenn ein Designwechsel eine Anordnung nicht nachzieht.

**Vierte Rückmeldungsrunde**

- **Deaktivierte Beschriftungen waren im dunklen Design kaum lesbar.** „Kulanzzeitraum (Minuten)" und „Neustart-Countdown vorher anzeigen (Minuten)" standen in Windows' eigenem Grau (#6D6D6D), weil WinForms eine deaktivierte Beschriftung **immer** so zeichnet und jede Designfarbe dabei ignoriert — auf dem dunklen Kartengrund gemessene 3,56 : 1. Eine Beschriftung muss aber nicht deaktiviert sein: sie nimmt keine Eingaben an und liegt nicht im Tabulator-Weg. Sie wird jetzt **gedämpft statt deaktiviert**, in einer aus dem Design berechneten Farbe. Nachgemessen über alle sieben Designs: **6,23 : 1 im dunklen** (statt 3,56), mindestens 3,95 : 1 überall sonst — durchweg besser als Windows' Grau. Die Layout-Messung prüft den Kontrast jeder Beschriftung in jedem Design jetzt mit, und ein Test verhindert, dass wieder jemand `Enabled = $false` auf eine Beschriftung setzt.
- **Die Suche nach Programm-Updates sagt jetzt, worum es geht — und was ein 404 bedeutet.** „Nach Updates suchen…" stand direkt neben den Zeilen des Intune-Update-Scans und war davon nicht zu unterscheiden; jetzt heißt es „Suche nach einer neueren Version von WinTuner GUI selbst", und die Protokollzeile nennt die abgefragte Adresse. Antwortet GitHub mit **404**, steht nicht mehr „bitte Internetverbindung prüfen" da, sondern dass es unter diesem Repository kein Release gibt — Name falsch (umbenannt?) oder noch nichts veröffentlicht.
- **Die optionale Gruppen-Berechtigung wird selbst erteilt, nicht „angefordert".** Der alte Ja/Nein-Dialog fragte „Berechtigung jetzt anfordern?" und las sich, als müsse jemand anders sie erteilen. Tatsächlich öffnet sich ein Anmeldefenster, in dem ein Konto mit den passenden Rechten die Zustimmung **selbst** gibt — für sich oder für die ganze Organisation. Der Dialog sagt das jetzt und bietet drei Wege: mit dem angemeldeten Konto erteilen, **mit einem anderen Konto anmelden** (Administratorrechte liegen beim Dienstleister oft auf einem zweiten Konto), oder abbrechen und die Gruppen-ID einfügen. Für den zweiten Weg wird nur der Token-Cache der Graph-PowerShell geleert, **nicht** der von WinTuner — die Tenant-Verbindung bleibt bestehen. Und wenn Microsoft mit „Genehmigung erforderlich" antwortet, sagt die Meldung genau das, statt den rohen Fehlertext zu zeigen.

- **Die Einstellungsseite sprang nach dem Scrollen.** Wer heruntergescrollt hatte und wieder hoch, fand oben einen leeren Block und alle Karten zu tief. Ursache: in einem scrollenden Bereich sind die Koordinaten der Karten **relativ zum gescrollten Ursprung** — „Karte auf 48" landet bei 400 px Scrollweg in Wahrheit auf 448. Ausgelöst hat das jede Neuanordnung im gescrollten Zustand: eine Änderung der Fenstergröße, ein Designwechsel oder das erneute Öffnen des Bereichs, der seine Scrollposition behält. Nachgestellt und gemessen (Karten standen danach auf 448 statt 48), behoben für Einstellungen, Store und die gestapelten Karten. Die neue Layout-Messung prüft das jetzt mit: sie scrollt jeden Bereich, ordnet neu an und meldet jede Karte, die sich dabei verschiebt.

- **„Paketerstellung fehlgeschlagen" sagt jetzt, woran es lag.** Der Grund stand im Ergebnis des Paketbaus und wurde weggeworfen — im Protokoll blieb eine einzige Zeile ohne ein Wort zur Ursache. Jetzt stehen Statuszeile und Protokolleintrag mit der Meldung des Moduls da.

- **Drei Überlappungen in der Oberfläche beseitigt — und eine Messung dagegen eingezogen.** In „Alle Tenant-Apps" lag der Hinweistext mitten in der Knopfreihe, das Detailfeld über dem dritten Knopf (der beim Vergrößern des Fensters als einziger nicht mitwanderte) und die Beschriftung einen Pixel im Detailfeld. In „App-Zuweisungseinstellungen" lag „Neustart-Countdown vorher anzeigen (Minuten):" über seinem Eingabefeld, und „Auto-Update abgelöster Versionen:" verdeckte seine Auswahlliste vollständig — die Liste war im Bild schlicht nicht vorhanden. Ursache überall: handgezählte Pixelkoordinaten aus einem 720×790-Entwurf. Die Anordnung wird jetzt gemessen. Neu ist `tests/LayoutProbe.ps1`: startet das gebaute Skript, geht jeden Bereich bei zwei Fenstergrößen durch und meldet jedes Paar Steuerelemente, das sich überlappt — in beiden Arbeitsabläufen (`validate`, `release`) als Pflichtschritt.
- **„App-Zuweisungseinstellungen" braucht keine Bildlaufleiste mehr.** Der Editor bekam immer seine Dialoghöhe von 790 px, auch wenn der Bereich nur 625 px hoch war: der Knopf „Auf markierte Apps anwenden" lag unter der Kante, ohne dass eine Bildlaufleiste darauf hinwies. Jetzt richtet er sich nach dem vorhandenen Platz — die Beschriftungsspalte beginnt hinter der breitesten Beschriftung, die Statuszeile steht neben dem Knopf statt darunter, und ab genügend Breite stehen die Einstellungen in **zwei Spalten**. Die Liste bekommt, was übrig bleibt: auf einem 1920er Bildschirm 457 px statt 220. Reicht der Platz trotzdem nicht, wächst der Bereich und scrollt — abgeschnitten wird nichts mehr.
- **Die Liste unter „Lokale Pakete" wächst mit dem Fenster.** Sie stand auf festen 108 px — gut vier Zeilen —, während darunter 350 px leer blieben und man in einer halbleeren Seite scrollte.

- **Die Selbstaktualisierung ist scharf geschaltet.** In `src/05-Config.ps1` steht jetzt `zTeck-arch/verteilwerk`, deshalb sucht auch eine selbst gebaute Kopie nach neueren Fassungen – Knopf „Nach Updates suchen" und die abschaltbare Startsuche. Für heruntergeladene Fassungen war das nie ein Problem: der Release-Ablauf trägt das Repository beim Bauen des Artefakts selbst ein. Am Sicherheitsweg ändert sich nichts – nur HTTPS, nur GitHub-Hosts, `.sha256`-Asset Pflicht, Prüfsumme wird verglichen, zwei Sicherungen vor dem Ersetzen.

- **Kein Fortschrittsbalken mehr, sondern eine Prozentangabe.** Der Balken war eine Behauptung: Paketieren und Hochladen laufen auf dem UI-Thread, und währenddessen pumpt niemand die Nachrichtenschleife – der Marquee stand still, der fortlaufende Balken blieb minutenlang auf demselben Wert. Beides las sich wie ein hängendes Programm. Jetzt steht dort „Fortschritt: 45 % (5 von 11 erledigt)", gerechnet aus **erledigten** Schritten und abgerundet, damit nie 100 % dasteht, solange noch etwas läuft. Wo die Stückzahl nicht bekannt ist (früher der Marquee), steht nur, dass etwas läuft.
- **Der Knopf „Nach aktueller App stoppen" ist weg.** Er saß neben genau dem Balken, der während der langen Schritte einfror; ein Klick wurde erst nach dem laufenden Upload überhaupt sichtbar. Der Merker dahinter bleibt für den Programmschluss: wird das Fenster geschlossen, endet ein Lauf zwischen zwei Apps statt mitten im Upload.
- **Der Bereich „App-Zuweisungseinstellungen" funktionierte gar nicht.** Beim Öffnen stand im Protokoll „App settings load failed: The property 'Text' cannot be found on this object", und „App-Liste neu laden" beendete die Oberfläche mit „The expression after '&' … must result in a command name". Ursache: derselbe Editor wird als Fenster **und** als Bereich gebaut; als Bereich kehrt die aufbauende Funktion sofort zurück, und danach sahen alle Ereignisse ihre Steuerelemente als `$null`. Die Steuerelemente liegen jetzt in einem Zustandsbeutel im Skript-Bereich; der modale Dialog legt seinen eigenen an und stellt den vorherigen beim Schließen zurück. `.GetNewClosure()` wäre der naheliegende Griff und ist bewusst **nicht** verwendet – in einer Closure zeigt `$script:` woanders hin und die Funktionen des Skripts werden nur gefunden, solange das Skript zufällig das oberste ist. Als Test hinterlegt, samt dem Sprachverhalten dahinter.
- **„App-Liste neu laden" steht jetzt als erster Knopf** vor „Alle auswählen" und „Alle abwählen" – erst die Liste holen, dann darin auswählen. Als Bereich ist die Liste beim Öffnen leer, solange keine Verbindung besteht.
- **403 bei den erkannten Apps sagt jetzt, was fehlt.** Die Liste kommt aus `deviceManagement/detectedApps` und braucht die Graph-Berechtigung `DeviceManagementManagedDevices.Read.All` – die hat mit Paketieren und Bereitstellen nichts zu tun, alle anderen Bereiche arbeiten ohne sie weiter. Statt „Response status code does not indicate success: 403 (Forbidden)" steht in Statuszeile und Protokoll jetzt, welche Berechtigung fehlt und wer sie erteilen kann.

**Dritte Rückmeldungsrunde**

- **Der Sammel-Editor für Bereitstellungseinstellungen ist ein eigener Bereich** unter „Bestand" statt eines Modaldialogs hinter „Extras". Für ein Feature, mit dem man Benachrichtigungen, Verfügbarkeit, Fristen, Neustartverhalten und Zustellpriorität für beliebig viele Apps auf einmal setzt, war ein Fenster zu wenig Platz. Der Dialogweg bleibt bestehen: der Einzelfall-Knopf in „Alle Tenant-Apps" ruft dieselbe Funktion weiterhin als Fenster auf.
- **Die beiden Store-Karten tauschten beim Aufklappen der erweiterten Optionen die Plätze.** Regression aus dieser Version: der Aufklapper setzte die App-Karte selbst unter die Zuweisungskarte – richtig, solange die Zuweisung oben stand, falsch seit dem Tausch. Die Anordnung liegt jetzt an einer Stelle, der Aufklapper ändert nur noch die Höhe seiner eigenen Karte.
- **Das Info-Symbol überlappte den Kartentitel.** Es wurde einmal bei der Erzeugung neben die Beschriftung gesetzt; wechselt danach die Schriftart – und das macht jeder Designwechsel –, wird der Text breiter und der letzte Buchstabe lief in den Kreis. Sichtbar an „Erkennungsregel ermitteln". Alle 28 Symbole werden jetzt nach jedem Schriftwechsel neu gesetzt; nachgemessen über alle sieben Designs, keine Überlappung.
- **Die Nummern an „Erkannte Apps scannen" und „Markierte Apps bereitstellen" sind weg.** Zwei Schritte, deren Reihenfolge feststeht und deren zweiter bis zum Ergebnis des ersten deaktiviert ist – die Nummern sagten nichts, was der Zustand nicht schon sagt.
- **Eine Auswahlliste stand aus der Flucht.** In den Bereitstellungseinstellungen saß „Auto-Update abgelöster Versionen" auf x=400, alle anderen auf x=240 – die Beschriftung war mit 314 px zu breit. Sie ist gekürzt, die Liste steht in der Flucht.
- **Kein abgeschnittener Text mehr im ganzen Programm.** Alle Bereiche wurden maschinell nach Beschriftungen durchsucht, die breiter sind als ihr Steuerelement; der letzte Fund fehlte um ein Pixel.
- **Einmaliger Hinweis auf den neuen Ort des Repositories** beim ersten Start nach dem Update. Nur für Bestandsnutzer – wer heute zum ersten Mal startet, hat nie einen alten Ort gekannt. Wer aus WinTuner GUI kommt, findet die Adresse im ohnehin erscheinenden Übernahme-Dialog; zwei Dialoge beim Start sind einer zu viel.

**Zweite Rückmeldungsrunde**

- **Die Update-Liste war zu klein und brauchte eine Bildlaufleiste bei zwei Treffern.** Eine Regression aus dieser Version: die Karte der abgelösten Apps war für die neue Auswahlliste auf 220 px festgesetzt worden und die Versionsbestand-Karte kam mit 101 px dazu – zusammen 200 px, die vorher der elastischen Update-Liste gehörten. Bei einem 853 px hohen Fenster blieben ihr 97 px. Die Abgelöste-Karte bemisst sich jetzt nach ihrem Inhalt (drei Zeilen leer, bis acht bei Inhalt), die Update-Liste bekommt den Rest: 135 statt 97 px bei derselben Fenstergröße. Dazu ein zweiter Fehler: die Spaltenverteilung rechnete die vertikale Bildlaufleiste nicht ab, wodurch 1577 px Spalten in 1568 px Sichtfläche standen – daher auch die waagerechte Leiste.
- **Die Store-Liste und die Tenant-App-Liste wachsen jetzt mit dem Fenster.** Die Store-Liste stand auf festen 126 px, bei der Tenant-Liste wuchs nur die Breite mit, nicht die Höhe – auf einem großen Bildschirm eine schmale Zeile Inhalt in einer halbleeren Karte.
- **Beim Löschen abgelöster Apps wird angesagt, was gerade geprüft wird.** Die zwei Sonden fragen bis zu drei Graph-Endpunkte ab; antworten die nicht, dauert das mit Zeitüberläufen über zehn Sekunden, in denen das Fenster stillstand und wie eingefroren aussah.
- **„Behalten" sagt jetzt, WARUM.** Die Meldung fasste zwei grundverschiedene Fälle zusammen: „noch zugewiesen oder installiert" und „Intune hat nicht geantwortet, der Zustand ist unbekannt". Im ersten Fall ist alles in Ordnung, im zweiten weiß man schlicht nichts – wer das nicht unterscheiden kann, weiß nicht, ob er warten oder etwas reparieren muss. Beide Fälle haben eigene Meldungen und eigene Protokollzeilen mit den Rückgaben der Sonden.
- **Das Sicherheitsnetz steht jetzt mit fetter Kopfzeile in den Einstellungen** und nennt den eigentlichen Punkt: eine alte Version verschwindet erst, wenn sie wirklich abgelöst IST – nicht schon, weil eine neuere existiert.
- **Die Suche nach Programm-Updates beim Start lässt sich abschalten.** Sie lief fest bei jedem Start, sobald ein Repository konfiguriert ist, und daneben stand ein Knopf, der dasselbe von Hand tut – zwei Wege zu einer Sache und keiner, den automatischen abzustellen. Standard bleibt eingeschaltet, das entspricht dem bisherigen Verhalten.
- **Die Seitenleiste hat eine dritte Gruppe „Dieser Rechner"** mit „Lokale Pakete" und „Einstellungen". Damit hat auch „Einstellungen" eine Überschrift, und „Lokale Pakete" steht nicht mehr unter „Neu bereitstellen", wo es nie hingehörte – es legt nichts in Intune an. Die Leiste kann jetzt außerdem scrollen: sie stand auf `Dock = Fill` ohne `AutoScroll`, Einträge unterhalb der Fensterhöhe wären ersatzlos unerreichbar gewesen.
- **Der Sammel-Editor für Bereitstellungseinstellungen steht jetzt in „Alle Tenant-Apps".** Er war nur über das Extras-Menü erreichbar, während der Einzelfall längst als Knopf in genau der Sektion stand, in der man ihn sucht.

**Aus der Rückmeldung zur ersten Fassung**

- **Im dunklen Design war das Untermenü „Ansicht › Design" praktisch unlesbar.** Eine Regression aus dem Menü-Umbau derselben Version: die Einfärbung ging genau zwei Ebenen tief. Solange „Design" und „Sprache" auf oberster Ebene standen, lagen ihre Einträge auf Ebene zwei und waren erfasst; unter „Ansicht" liegen sie auf Ebene DREI und behielten die Systemfarbe – dunkelgrau auf dunklem Grund. Die Einfärbung läuft jetzt rekursiv, deaktivierte Einträge bekommen die gedämpfte Farbe. Nachgemessen über alle sieben Designs: niedrigstes Kontrastverhältnis 13,2 : 1, keine Systemfarbe mehr im Baum. Als Test hinterlegt, samt einer künstlichen dritten Ebene.
- **Die Sicherheitsprüfung vor dem Löschen steht jetzt als eigene Aussage in den Einstellungen.** Sie war immer da und ist nicht abschaltbar – aber sie stand nur verstreut in den Hinweisen der einzelnen Optionen und sah damit aus wie eine Eigenschaft der jeweiligen Option statt wie eine Regel, die immer gilt. Wer in den Einstellungen danach sucht, fand sie nicht. Jetzt über beiden Lösch-Optionen: eine Version wird nur gelöscht, wenn Intune null Zuweisungen **und** null Geräte mit erfolgreicher Installation meldet; meldet auch nur ein Gerät die alte Version noch, bleibt sie stehen. Eine gemeldete Null wird gegen eine zweite, unabhängige Quelle gegengeprüft, und lässt sich der Zustand nicht ermitteln, gilt er als „installiert".
- **Ein WinGet-Paket lässt sich auf dem eigenen Rechner installieren.** Neuer Knopf „Auf diesem PC installieren…" in der Suchkarte. Er ruft `winget install` für das ausgewählte Paket – nicht die gebaute .intunewin, die für den Systemkontext gemacht ist und von Hand ein anderes Ergebnis liefern würde. Mit Rückfrage, die den Rechnernamen nennt, mit Vorabprüfung, ob winget überhaupt vorhanden ist, und mit Auswertung des Rückgabewerts („schon aktuell" ist kein Fehler).
- **Die Autostart-Option der lokalen Pakete ist in die Einstellungen umgezogen.** Sie ist eine Startentscheidung wie „nach der Anmeldung nach Updates suchen" und stand als einzige davon nicht auf der Einstellungsseite, sondern unter einer Liste – und speicherte sich deshalb beim Anhaken statt beim Speichern. Jetzt in der Karte „Paketierung auf diesem Rechner", mit derselben Regel wie alles dort.
- **Das Extras-Menü ist nach Wirkungsbereich gruppiert** statt in der Reihenfolge, in der die Einträge über die Zeit dazukamen: was auf den Tenant wirkt, was nur auf diesen Rechner wirkt, und die Auswertung – getrennt durch Linien. Die Position bleibt: das sind Aktionen, keine Einstellungen. Dass „Protokolldatei öffnen" und „Versions-Cache leeren" zusätzlich als Knopf auf der Einstellungsseite stehen, bleibt ebenfalls – im Störungsfall sucht man das Protokoll im Menü und nicht auf einer Seite, die man dafür erst aufschlagen müsste.

**Name bleibt: WinTuner GUI**

- **Die Umbenennung zu „Verteilwerk" ist zurückgenommen.** Eine Zwischenfassung dieser Version trug den Namen; sie wurde nie veröffentlicht. Alles heißt wieder wie in 0.15.8: die ausgelieferte Datei `WinTuner_GUI_ntg.ps1`, das Repository `zTeck-arch/wintuner_gui`, die Datenordner `%APPDATA%\WinTunerGUI` und `%LOCALAPPDATA%\WinTunerGUI`, die Protokolle `WinTuner_GUI_JJJJ-Www.log`. Damit findet die Selbstaktualisierung bestehender 0.15.8-Installationen ihr Asset unter dem erwarteten Namen, und niemand muss Daten umziehen.
- **Wer die Zwischenfassung benutzt hat, verliert nichts.** Die Datenübernahme, die für die Umbenennung gebaut wurde, läuft in die andere Richtung weiter: fehlt `%APPDATA%\WinTunerGUI\settings.json` und liegt eine Datei unter `%APPDATA%\Verteilwerk`, wird sie einmalig **kopiert** (nicht verschoben) und beim Start genannt. Dasselbe gilt für den Leistungsnachweis.
- **Der Umzugshinweis auf ein neues Repository ist entfallen** — das Repository hat nie seinen Namen gewechselt.
- **Der Kundenname steht in der Titelleiste.** Bei zwei offenen Fenstern für zwei Tenants waren die in der Taskleiste bisher nicht zu unterscheiden – der teuerste Fehler dieses Werkzeugs ist, im Fenster des falschen Kunden zu arbeiten.

**Sicherheit und Reichweite von Aktionen**

- **„Alle Apps: neueste N behalten" stand in der falschen Karte.** Der Knopf saß in „Abgelöste (alte) App-Versionen", direkt neben einer Liste abgelöster Apps – wirkte aber auf **jede** verwaltete App im Tenant. Sein Tooltip musste die Karte um ihn herum richtigstellen. Er hat jetzt eine eigene Karte „Versionsbestand ALLER verwalteten Apps" mit einer Zeile zur Reichweite.
- **Abgelöste Apps lassen sich einzeln markieren und gemeinsam löschen.** Bisher gab es ein einzeiliges Auswahlfeld: eine App sichtbar, die Anzahl gar nicht, keine Mehrfachauswahl – während daneben „Alle abgelösten Apps löschen" stand. Man konnte also eine oder alle löschen und nichts dazwischen. Jetzt eine Liste mit Kästchen, „Alle auswählen"/„Alle abwählen" und einer Rückfrage, die alle betroffenen Apps namentlich nennt. Die Sicherheitsprüfung (null Zuweisungen **und** null erfolgreiche Installationen) gilt unverändert je App.
- **„Alle aktualisieren" tritt optisch zurück und nennt seine Zahl.** Vorher stand „ALLE aktualisieren (auch nicht ausgewählte)" gleich groß und gleich betont neben „Ausgewählte Apps aktualisieren", und die ganze Warnung hing an der Großschreibung. Jetzt steht „Alle 47 aktualisieren" da – die Zahl ist **vor** dem Klick sichtbar, nicht erst in der Rückfrage.
- **Doppelte Sicherung beim Sammel-Löschen entfernt.** `Save-AppScopeSnapshot` lief je gelöschter App zweimal mit identischen Argumenten: zwei Graph-Lesevorgänge und zwei Schnappschüsse statt einem.
- **Leere Listen sagen jetzt, warum sie leer sind.** Ohne Anmeldung stand dort „auf Suchen klicken, um zu laden" – genau das geht dann nicht, und der Klick führte erst hinterher zu einem Anmelde-Dialog. Jetzt: „Nicht verbunden. Oben im Fenster am Tenant anmelden."

**Aufbau der Oberfläche**

- **Die Einstellungsseite ist nach Wirkung gegliedert** statt in eine Karte „Allgemein": Paketierung auf diesem Rechner, Suche nach App-Updates, was nach einem Update mit der alten Version passiert, Rückfragen, Updates des Programms, Dateien und Diagnose. **Jede Option trägt eine Zeile, die sagt was sie tut, ob sie nur liest und was sie kostet.** Vorne steht der Satz, der bisher gefehlt hat: es gibt keine automatische App-Aktualisierung – ein Update wird immer von Hand gestartet.
- **Drei Schalter aus dem Extras-Menü sind dorthin umgezogen** (Zuweisungen vor dem Löschen sichern, Kachel mit echtem Versionsvergleich, Rückfragen überspringen). Im Menü speicherten sie sich beim Klick, während alles auf der Einstellungsseite auf „Speichern" wartete – dieselbe Art Einstellung mit zwei Verhalten, je nachdem wo man sie fand.
- **Die Menüleiste hat drei Menüs statt fünf gemischter Einträge**: Extras (mit dem Leistungsnachweis), Ansicht (Design und Sprache), Hilfe.
- **Die Seitenleiste ist gruppiert**: Übersicht – „Neu bereitstellen" (WinGet-Apps, Microsoft Store, Eigene Installer) – „Bestand" (Updates, Alle Tenant-Apps, Erkannte Apps) – Einstellungen.
- **„Lokale Pakete" ist ein eigener Bereich.** Die Karte steckte in „Apps aus WinGet hinzufügen", legt aber nichts in Intune an – sie pflegt lokale Paketkopien.
- **Der Store-Bereich liest sich vorwärts**: erst die App wählen, dann zuweisen, wie bei WinGet. Der Satz, der die umgekehrte Reihenfolge erklären musste, entfällt.
- **Ein Nummerierungsstil.** „Schritt 1 – …" und „1 · …" standen nebeneinander, und innerhalb von „Schritt 2" waren die Knöpfe wieder mit 1./2./3. nummeriert. Der CSV-Export bei den erkannten Apps war als „3." ausgewiesen, ist aber kein dritter Schritt.
- **Erklärt wird an einer Stelle statt an zweien.** Ein Satz unter dem Bedienelement, alles Weitere im Info-Symbol. Die drei Schrittkarten in „Eigene Installer" hatten als einzige gar kein Info-Symbol – deshalb trug der Absatz darunter vier bis fünf Zeilen, einer verwies sogar auf sein eigenes Tooltip.
- **Das Aktivitätsprotokoll startet eingeklappt**, der Zustand wird gemerkt. Aufgeklappt belegte es bis zu 40 % der Fensterhöhe und zeigte dabei dieselbe Zeile, die schon in der Statuszeile steht.
- **Die Übersicht trennt Tenant und Rechner.** „Lokale Pakete" ist Speicherplatz auf diesem Rechner und stand in einer Reihe mit drei Tenant-Zahlen – als einzige Kachel mit einem Wert, direkt neben dem Hinweis, man müsse sich erst verbinden.

**Abgeschnittene Beschriftungen und Layout**

- **„Bei Tenant anmelden" passte nie in seinen Knopf.** 118 px Text in 120 px – abgeschnitten, seit es die deutsche Fassung gibt. Dasselbe bei „Ausgewählte Apps aktualisieren", „Log für Fehlerbericht exportieren" und „<Alle Herausgeber>". Kopfzeile und Knopfreihen messen sich jetzt an ihren Beschriftungen statt an Pixelkonstanten.
- **Karten bekommen ihre Höhe aus dem Inhalt.** In „Eigene Installer" verschwinden damit 200 px tote Fläche. Vorher standen dort handgezählte Höhen mit Kommentaren wie „vier umgebrochene Zeilen; 62 px schnitten die letzte ab" – richtig für eine Schriftart und eine Textlänge.
- **Das Info-Symbol zerfiel in den Retro-Designs.** Es zeichnete sich mit fest verdrahteter Schrift; der themenweite Schriftwechsel schob Kreis und Buchstabe auseinander, sodass statt eines „i" im Kreis eine Klammer dastand.
- **Drei englische Beschriftungen im deutschen UI übersetzt** („<All Publishers>", „Device Count", „Alphabetical" bei den erkannten Apps).
- **Das Anmeldefeld heißt „Anmeldeadresse"**, wie die eigene Hilfe es durchgehend nennt – nicht mehr „Benutzername".

**Unter der Oberfläche**

- **Die längste Blockade verlässt den UI-Thread.** Gemessen: `GraphSession::Instance` ist ein prozessweites Singleton, ein zweiter Runspace sieht dieselbe Anmeldung. Der früher entfernte BackgroundWorker scheiterte am fehlenden PowerShell-Runspace, nicht am Graph-Kontext – daraus war im Quelltext ein Naturgesetz geworden. Die Inventar-Abfrage läuft jetzt im vorhandenen Paket-Runspace, das Fenster zeichnet währenddessen. Mit Rückfall auf den bisherigen Weg; Fehler werden unverändert weitergereicht, damit Wiederholungslogik und Truncation-Prüfung genau so greifen wie zuvor.
- **Die Zuweisungs-Semantik steht nur noch einmal da.** Benachrichtigungsstufen, Fristen, unzulässige Neustart-Kombinationen und „nichts zu ändern → gar nichts schicken" waren für den WinGet- und den Store-Weg Zeile für Zeile dupliziert; eine Korrektur an einer Kopie wäre in der anderen unbemerkt ausgeblieben. Dass der Store-Weg schmaler ist, ist jetzt ablesbar statt implizit.
- **Zwei neue Prüfungen im Build**, beide gegen Fehler, die während dieser Arbeit tatsächlich passiert sind: definierte, aber nirgends gelesene Sprachschlüssel (als Hinweis), und deutsche Anführungszeichen in einzeiligen Texten. Meist bricht schon der Parser ab, dann allerdings mit einer Zeilennummer in der zusammengebauten Datei statt mit dem Namen des Schlüssels. Der gefährlichere Fall ist der seltenere: schließt der Reststring zufällig wieder auf, entsteht ein gültiger, aber falscher Text – und den fängt nur diese Prüfung.
- **22 neue Testdateien** (21 Unit-Tests plus der Smoke-Test), insgesamt 37 Dateien und 391 Tests. Neu unter anderem: die Datenübernahme aus dem alten Namen, der geteilte Zuweisungsleser und die Runspace-Messung – letztere, damit die Aussage geprüft bleibt statt erinnert zu werden.

## 0.15.8 – Nachtrag zu 0.15.7: Die Update-Suche fand keine Updates mehr

- **Die Update-Suche meldete „keine Updates gefunden“, obwohl welche vorhanden waren.** Ab 0.15.7 wurde die App-Liste aus Intune mit dem Filter `-Update:$false` geladen. Dieser Parameter des WinTuner-Moduls bedeutet nicht „Updates mitprüfen“, sondern filtert **nach dem Update-Zustand**: `$false` liefert ausschließlich Apps, die schon aktuell sind. Genau die veralteten Apps, um die es geht, fehlten damit in der Liste, die die Suche prüft – sie kamen nie beim Versionsvergleich an. Im Kundentenant verschwanden so Java 8 und Jabra Direct (6.27.3702 → 8.1.14601) aus der Ansicht, obwohl 0.15.6 sie noch fand; die Übersicht zeigte weiterhin korrekt „3 Updates“, weil sie die Liste auf einem anderen Weg holt. Der Filter wird jetzt nur noch gesetzt, wenn er ausdrücklich verlangt wird.
- **Betroffen war auch die Versionsbereinigung.** Sie liest dieselbe Liste und sah dadurch nur aktuelle Apps, konnte also abgelöste Versionen übersehen. Mit demselben Fix behoben.

## 0.15.7 – Feldmeldungen: Tenants ohne aktive Apps, erster Fehlversuch beim Paketieren, klarere Fehlermeldungen

**Aus echten Kundenläufen**

- **Tenants, deren Apps alle abgelöst sind, luden die Update-Ansicht nicht mehr.** Bei einem Tenant mit 0 aktiven, aber 6 abgelösten Apps brach das Laden mit „Cannot bind argument to parameter 'ActiveApps' because it is an empty array" ab – ein leeres Feld wurde an der Parameterprüfung abgewiesen, bevor die eigentliche Logik lief. Der Fall wird jetzt zugelassen; ist keine aktive App vorhanden, erscheint schlicht „keine Apps", die abgelösten bleiben in ihrem Bereich sichtbar.
- **Der erste Klick auf „Aktualisieren" scheiterte, der zweite ging.** Das WinTuner-Modul zählt beim Bauen seine eigene, noch gefüllte App-Liste auf; unter der Oberfläche trat dabei „Collection was modified; enumeration operation may not execute" auf. Dieser Wettlauf ist vorübergehend – derselbe Aufruf klappt Sekunden später. Paketbau und Inventar-Abfragen (Update-Suche, Versionsbereinigung) wiederholen ihn jetzt automatisch mit kurzer Pause, statt den Vorgang abzubrechen. Man muss nicht mehr zweimal klicken.
- **„Request not applicable to target tenant" wird richtig benannt.** Diese Meldung bedeutet, dass der Tenant keine aktive Microsoft-Intune-Lizenz hat oder das Konto dort keine Intune-Berechtigung besitzt – ein dauerhafter Zustand. Bisher hieß es „möglicherweise vorübergehend nicht erreichbar, in einigen Minuten erneut versuchen", was in die falsche Richtung schickte. Jetzt steht dort, dass Wiederholen nicht hilft und Lizenz bzw. Rolle in genau diesem Tenant zu prüfen sind.

**Leistungsnachweis erfasst jetzt, was wirklich passiert**

- **Bereitstellungen und Zuweisungen werden endlich erfasst.** Bisher zählte der Nachweis nur Updates und Löschungen; neue Apps und die dabei gesetzten Zuweisungen tauchten nie auf – eine reine „neue Apps bereitstellen"-Sitzung las sich als „nichts erfasst". Jetzt werden festgehalten: jede Bereitstellung (WinGet, Microsoft Store, eigener Installer, erkannte Apps) unter „Neu bereitgestellt", und jede Zuweisungsänderung unter „Zuweisungen geändert" – neu angelegte Zuweisungen, geänderte Zuweisungseinstellungen und die automatische Übergabe an die neue Version beim Update.
- **Die irreführende Zeile „0 Zuweisungsänderungen" ist entfallen.** Statt einer Zahl, die nie erfasst wurde und daher immer 0 war, listet der Nachweis die tatsächlichen Zuweisungsänderungen jetzt einzeln im eigenen Abschnitt auf.

**Eigene Installer verständlicher**

- **Schritt 3 (Win32-App anlegen) hat jetzt ein eigenes, sichtbares Paketfeld.** Bisher zog sich diese Karte die `.intunewin` still aus einem Feld einer ganz anderen Karte weiter unten – man sah weder, welches Paket verwendet wird, noch konnte man dort ein anderes wählen. Jetzt steht die gewählte Datei sichtbar in der Karte, wird nach Schritt 1 automatisch eingetragen, und ein Knopf „Paketdatei wählen…" lässt **jede vorhandene** `.intunewin` auswählen – nicht nur die in dieser Sitzung gebaute.
- **Die Übernahme von Schritt 1 ist jetzt sichtbar.** Nach dem Bauen erscheint der Paketname direkt in Schritt 3 (und in der Ersetzen-Karte), statt wortlos zwei Karten tiefer zu landen.
- **Der Knopf „MSI-Informationen lesen" heißt jetzt „MSI: Daten automatisch auslesen"** und hat einen Tooltip, der erklärt, was er tut: liest bei einer MSI Produktcode, Version, Name und Herausgeber direkt aus der Datei – ohne Installation –, danach „Ins Formular unten übernehmen". EXE-Dateien nutzen weiterhin die drei nummerierten Schritte.
- **Neuer „Automatisch ermitteln"-Knopf in Schritt 2.** Ein Klick erledigt die ganze Erkennung bis zum Ergebnis: bei einer MSI wird sie direkt aus der Datei gelesen, bei einer EXE nacheinander Zustand aufnehmen, lokal installieren (mit einer Sicherheitsabfrage, weil dabei wirklich auf diesem Rechner installiert wird) und vergleichen. Das Ergebnis wird anschließend automatisch in die Win32-Karte übernommen. Die einzelnen Schritte bleiben für den manuellen Weg erhalten.
- **Klarere Wegweisung zwischen Installer und Paket.** Schritt 1 weist jetzt darauf hin, dass eine bereits vorhandene `.intunewin` direkt in Schritt 3 gewählt werden kann; Schritt 2 nennt im Hinweis, dass er den Installer aus Schritt 1 nutzt, nicht das Paket.
- **Alter Erkennungsstand bleibt nicht mehr stehen.** Wer erst eine MSI auslas und dann zu einer EXE wechselte, sah in Schritt 2 weiterhin die MSI-Daten von vorher – mit dem Risiko, sie versehentlich zu übernehmen. Beim Wechsel der Installer-Datei werden Ergebnisfeld und übernahmebereiter Fund jetzt geleert.

**Windows-Sandbox-Test friert die Oberfläche nicht mehr ein**

- **Kein Einfrieren mehr nach dem Sandbox-Test.** Die Modul-Funktion beendet ihren Ablauf mit einem blockierenden „Press enter when you closed the sandbox" – auf dem Oberflächen-Faden legte das die ganze Anwendung lahm („keine Rückmeldung"). Der Test läuft jetzt in einem eigenen Prozess mit geschlossener Eingabe: die Wartezeile erhält sofort ein Dateiende, die Sandbox startet, und das Fenster bleibt bedienbar. Auch die störende „WriteObject … cannot be called from outside"-Flut des Moduls bleibt in diesem Prozess.
- **Kein versehentlicher zweiter Start mehr.** Weil die Oberfläche einfror, klickten Nutzer ein zweites Mal – und ein zweiter Sandbox-Start neben dem ersten ist genau die Ursache für „Zugriff verweigert (0x80070005)", denn Windows erlaubt nur **eine** Sandbox gleichzeitig. Der Knopf wird während des Laufs gesperrt, und ist bereits eine Sandbox offen, erscheint jetzt eine klare Meldung statt des kryptischen Windows-Fehlers.
- **Der Installer wird für den Test in einen sauberen Ordner kopiert.** Windows Sandbox bindet den Ordner des Installers ein; liegt er in `Downloads`, `Dokumente` oder auf dem Desktop, blockiert der Ordnerschutz (Controlled Folder Access) das Einbinden häufig mit „Zugriff verweigert (0x80070005)". Der Test läuft jetzt aus einer Kopie unter `%LOCALAPPDATA%` – ein Ordner, der nicht geschützt ist –, was diese Ursache umgeht. Alte Kopien werden beim nächsten Lauf aufgeräumt.
- **Bei „Zugriff verweigert" hilft die Anwendung weiter** und nennt die üblichen Ursachen (noch offene Sandbox, geschützter Quellordner, Sicherheitsrichtlinie), statt den Nutzer mit dem Windows-Fehler allein zu lassen. Ausgaben des Hilfsprozesses werden vor der Anzeige von Farbcodes bereinigt.

**Protokolle ausführlicher**

- **Fehler werden im Protokoll jetzt vollständig benannt.** Bislang stand dort nur die oberste Fehlermeldung – oft die harmlose Hülle um die eigentliche Ursache, die in einer inneren Ausnahme steckt. Neu wird an den wichtigen Stellen (Anmeldung, Update-Lauf, Versionsbereinigung, App-Laden) eine einzeilige, durchsuchbare Vollangabe geschrieben: Fehlertyp, Meldung, innere Ausnahme(n), HTTP-Status und die Codezeile. So lässt sich ein Bindungsfehler, ein Graph-Fehler und ein Modul-Wettlauf im Nachhinein auseinanderhalten, statt jedes Mal dasselbe Symptom zu sehen.
- Wiederholte Modul-Wettläufe werden mit Versuchszähler protokolliert, und die Einordnung der ersten Intune-Abfrage nach der Anmeldung (dauerhaft vs. vorübergehend) steht jetzt im Log.

## 0.15.6 – Eigene Installer erklären sich selbst, Protokolle ins Benutzerprofil, englische README

**Eigene Installer**

- **Der Deinstallationsbefehl wird nicht mehr weggeworfen.** Er steht als `UninstallString` beziehungsweise `QuietUninstallString` direkt in dem Registry-Schlüssel, den der Abgleich ohnehin liest. Bevorzugt wird die stille Variante, denn die gewöhnliche öffnet meist ein Fenster, und eine Intune-Deinstallation, die auf einen Klick wartet, wird nie fertig.
- **„Ins Formular unten übernehmen".** Was der Erkennungsschritt herausfindet, landete bisher als Text zum Abtippen in einem Feld. Jetzt füllt ein Knopf Name, Herausgeber, Deinstallationsbefehl und die vollständige Erkennungsregel in die Win32-Karte, inklusive des 32-Bit-Schalters, wenn der Eintrag unter `WOW6432Node` liegt. Übernommen wird nur vorbelegt; angelegt wird die App weiterhin über ihren eigenen Knopf.
- **MSI vollautomatisch.** Ein Klick liest Produktcode, Version, Name und Herausgeber direkt aus der Datei und leitet daraus Installations- und Deinstallationsbefehl sowie die Erkennungsregel ab, ohne die Software installieren zu müssen. Gelesen wird über die Windows-Installer-Schnittstelle, also dieselbe Quelle, aus der auch der Installer selbst schöpft.


- **Paketieren scheiterte stumm, wenn Ziel- und Quellordner derselbe waren.** Die Paketierung komprimiert den gesamten Quellordner und weigert sich deshalb, ihr Ergebnis genau dorthin zu schreiben. Sie meldet das aber nur als `WARNING`, was an `-ErrorAction Stop` vorbeigeht: Der Aufruf kehrte nach 0,0 Sekunden zurück, meldete Erfolg, und übrig blieb die Meldung „hat keine .intunewin-Datei erzeugt" – das Symptom, nicht die Ursache. Der Fall wird jetzt vorher abgefangen und benannt, einschließlich eines Zielordners *innerhalb* der Quelle.
- **Die Paketierung sagt jetzt, was sie tut.** Alle Ausgabeströme des Werkzeugs werden protokolliert, dazu die Laufzeit. Ein verweigerter Lauf ist an unter einer Sekunde erkennbar, ein echter dauert Sekunden. Bleibt die Datei aus, nennt die Fehlermeldung die tatsächliche Meldung des Werkzeugs.
- **Die drei Pfadfelder sind entflochten.** Zuerst wird der Installer gewählt, der Quellordner ergibt sich daraus. Beide Regeln – Installer im Quellordner, Zielordner nicht darin – werden beim Tippen und Auswählen laufend geprüft und als Warnung angezeigt, statt erst beim Bauen aufzutreten.
- **Das fertige Paket wird sichtbar weitergereicht.** Der Pfad landete schon vorher automatisch in der Karte darunter, nur stumm und zwei Karten entfernt, sodass der Bau ins Leere zu führen schien.
- **Schritt 2 sagt, womit er arbeitet.** Die Erkennungsregel wird aus dem *Installer* ermittelt, nicht aus dem Paket, denn eine Regel lässt sich nur durch Installieren finden. Titel und Hinweis benennen das jetzt.
- **Leere Felder erzeugen keine Entwicklermeldung mehr.** Ein leeres Pfadfeld führte zu „Cannot bind argument to parameter 'SourcePath' because it is an empty string", weil PowerShell schon bei der Parameterbindung abbrach, bevor die eigenen Prüfungen liefen.
- **Windows Sandbox: Verfügbarkeit wird vorher geprüft.** Fehlte das Feature, meldete die Anwendung einen Fehler über ein Arbeitsverzeichnis, das mit der Ursache nichts zu tun hatte. Jetzt wird erklärt, was fehlt, und angeboten, es per `dism.exe` zu aktivieren oder den Windows-Features-Dialog zu öffnen. Voreingestellt ist „Abbrechen", denn beides verändert das System.
- Die Suche nach der erzeugten `.intunewin` läuft rekursiv, damit ein in einem Unterordner abgelegtes Paket nicht übersehen wird.

**Oberfläche**

- **Das Dashboard kann jetzt etwas.** Die drei Schnellaktionen sprangen bisher nur in einen Bereich. Dazugekommen sind drei, die tatsächlich arbeiten: Favoriten prüfen, alle lokalen Apps aktualisieren, Leistungsnachweis anzeigen. Alle drei arbeiten lokal oder lesend; alles, was Intune verändert, bleibt bewusst hinter seiner Sektion mit Rückfrage.
- **Vierte Kachel für den lokalen Paketordner** mit dem belegten Speicher, verlinkt auf die Einstellungen, wo aufgeräumt wird. Sie funktioniert auch ohne Tenant-Verbindung.


- **„Benutzername:" überlappte das Eingabefeld.** Die Beschriftung stand auf einer festen Position mit 76 Pixeln Platz; das deutsche Wort braucht 86. Sie wird jetzt gemessen und rechtsbündig vor das Feld gesetzt, was auch bei anderer Schriftgröße oder höherer DPI-Einstellung trägt.
- **Das Fenster ist breit genug für die vierte Kachel.** Bei 1014 Pixeln wurde die neue Speicher-Kachel rechts abgeschnitten; das Startmaß ist auf 1060 erhöht und wird jetzt aus der Mindestgröße des Fensters hergeleitet, statt an zwei Stellen fest verdrahtet zu sein.
- **Der Fortschritt beim Update-Lauf hängt nicht mehr scheinbar fest.** Der Balken war ein Laufband (Marquee), das nur animiert, solange der Oberflächen-Faden seine Nachrichtenschleife pumpt – während des langen Paketbaus ruckelte es, während des blockierenden Uploads stand es still, was wie ein Absturz aussah. Ein echter Prozentwert ist nicht möglich, weil weder WinGet noch das Modul einen liefern. Stattdessen füllt sich der Balken jetzt echt über die Anzahl der Apps (eine App fertig = ein Schritt), und die Statuszeile nennt die Phase je App: „(2/8) Google Chrome — Paketieren…" bzw. „— Hochladen zu Intune…". So ist jederzeit ablesbar, wo der Lauf steht, auch wenn eine einzelne große App minutenlang paketiert.

**Sicherheit und Protokolle**

- **Die Protokolle liegen nicht mehr neben dem Skript.** Bei der dokumentierten Nutzung war das der Downloads-Ordner – ein ungeeigneter Ort für diesen Inhalt: über drei Wochen gemessen enthielten die Protokolle 137 verschiedene Intune-App- und Entra-Gruppen-Kennungen aus mehreren Kunden-Tenants, im Klartext. Sie liegen jetzt unter `%LOCALAPPDATA%\WinTunerGUI\Logs`, also innerhalb des Benutzerprofils und ACL-geschützt wie der Paketordner. `Write-Log` beachtete die eingestellte Verzeichnisangabe bislang gar nicht und leitete den Pfad weiterhin aus `$PSScriptRoot` her; es gibt jetzt eine einzige Stelle, die über den Ort entscheidet.
- **Protokolle werden nach zwei Wochen gelöscht.** Ohne Frist wächst die Sammlung aus Kunden-Gruppenkennungen und App-Inventaren jahrelang auf dem Rechner eines Technikers. Entschieden wird über die im Dateinamen kodierte Kalenderwoche, nicht über den Zeitstempel der Datei – ein Kopieren oder Synchronisieren schreibt den Zeitstempel neu und würde alten Inhalt sonst unbegrenzt am Leben halten. Das Aufräumen läuft beim Start, wird protokolliert und darf den Start nie verhindern.
- **Nur ein Schreiber gleichzeitig.** `Add-Content` öffnet, schreibt und schließt ohne Sperre; zwei laufende Instanzen verschränkten ihre Zeilen und konnten sich gegenseitig abschneiden – genau die Beschädigung, die ein Protokoll im Moment des Bedarfs unbrauchbar macht. Ein benannter Mutex schützt das jetzt; wer die Sperre nicht binnen einer Sekunde bekommt, überspringt die Datei und zeigt die Zeile trotzdem im Fenster.
- **Sicherheitsrichtlinie (`SECURITY.md`).** Wie Schwachstellen privat gemeldet werden (GitHubs private Meldefunktion sowie eine Kontaktadresse), was im Geltungsbereich liegt und was bewusst nicht, und welche Eigenschaften bekannt und beabsichtigt sind – etwa das nicht signierte Skript, der zwischengespeicherte Anmeldevorgang und der Klartext-Inhalt der lokalen Protokolle.

**Dokumentation**

- **Die README ist jetzt englisch**, die deutsche Fassung liegt als `README.de.md` daneben, mit Sprachumschalter oben in beiden. Überarbeitet, mit Inhaltsverzeichnis und ohne Gedankenstriche.
- Der Hinweis auf ältere Versionen ohne funktionierende Update-Prüfung ist entfallen; solche Kopien gibt es nicht mehr.

## 0.15.5 – Microsoft Store als eigener Bereich, Gruppen-Favoriten, verständlichere Berichte

**Die Veröffentlichung hat noch nie funktioniert**

- **Kein einziges Release konnte gebaut werden.** Der Workflow setzt nach dem Bauen die Repository-Kennung in die ausgelieferte Datei ein. Das dafür verwendete Suchmuster endete auf `\s*$`; da `\s` auch den Wagenrücklauf einschließt und `$` im Mehrzeilenmodus **vor** dem Zeilenvorschub steht, verschluckte der Treffer das `\r`. Der Ersatztext setzte keines zurück, und genau diese eine Zeile endete danach mit einem nackten `\n`. Seit 0.15.4 laufen die Static Checks bewusst **nach** diesem Eingriff – und lehnten das Ergebnis seitdem jedes Mal ab. Das Muster prüft das Zeilenende jetzt über einen Vorausschau-Ausdruck, ohne es zu verbrauchen. Frühere Releases enthielten dieses nackte `\n` unbemerkt.

**Microsoft Store**

- **Eigener Bereich in der Navigation.** Store-Apps werden direkt in Intune angelegt, es wird nichts paketiert oder hochgeladen – als dritte Karte unter „WinGet-Apps" war das fachlich falsch einsortiert. Der Bereich hat jetzt eine eigene Zuweisungskarte; bisher lieh er sich die Steuerelemente der Nachbarkarte, weshalb der Hinweis „oben unter *Zuweisen an*" auf eine Karte mit ganz anderem Titel zeigte.
- **Die bereitgestellte App bleibt im Blick.** Nach dem Bereitstellen wurde das Suchfeld geleert und die Liste ungefiltert neu geladen, sodass sie auf sämtliche Store-Apps des Tenants aufklappte und die gerade angelegte darin unterging. Jetzt wird nach der Paket-ID gefiltert und die neue App markiert.
- **Keine geliehenen Einstellungen mehr.** Filter, Fristen und Kategorien aus der WinGet-Karte wirkten still auf eine danach bereitgestellte Store-App. Der Bereich hat eigene Felder. Verfügbarkeitsdatum, Übermittlungsoptimierung und Auto-Update werden nicht mehr angeboten – Store-Apps unterstützen sie nicht, was bisher erst hinterher als Warnung erschien.

**Zuweisungen**

- **Gruppen-Favoriten je Kunde.** Häufig gebrauchte Entra-Gruppen lassen sich neben dem Gruppenfeld unter einem Namen speichern und erscheinen anschließend direkt in der Ziel-Auswahlliste. Die Favoriten sind an die Tenant-Domain gebunden: Gruppen eines Kunden tauchen bei einem anderen nie auf. Verfügbar an allen drei Stellen mit Zielauswahl.
- **Ein stiller Datenverlust beim Speichern.** `Save-Settings` serialisierte ohne Tiefenangabe; der Standard von zwei Ebenen hätte jeden Favoriten beim ersten Speichern in die Zeichenkette `System.Collections.Hashtable` verwandelt.

**Berichte und Rückmeldungen**

- **Der Leistungsnachweis zählt Updates statt Objekte.** Gingen mehrere alte Versionen auf dieselbe neue über, stand die App mehrfach untereinander und las sich, als wäre sie mehrfach aktualisiert worden. Jetzt eine Zeile je App und Zielversion, Vorgänger älteste zuerst.
- **„Bewusst behalten" ist kein Fehler mehr.** Die Bereinigung meldete Schutz und Fehler in derselben Zahl. Beides ist getrennt, und wer die Bereinigung selbst anstößt, bekommt je Version den konkreten Grund genannt – samt Hinweis, dass sich diese Versionen von selbst erledigen, sobald die Geräte gewechselt haben.

**Kleinere Verbesserungen**

- **Der Update-Suchlauf nach dem Login ist standardmäßig aus.** In großen Tenants durchsuchte er nach jeder Anmeldung jede App. Bereits gespeicherte Einstellungen behalten ihren Wert.
- Hinweis am Benachrichtigungsfeld, dass „unverändert" Intunes eigene Vorgabe bedeutet, und die ist „alle Popup-Benachrichtigungen anzeigen".
- Info-Symbol bei „Inhalt einer vorhandenen App ersetzen", das auch erklärt, wann man es *nicht* nehmen sollte und dass Erkennungsregeln dabei unangetastet bleiben.
- README erklärt jetzt, wie der Anmelde-Zwischenspeicher technisch funktioniert und was er für gemeinsam genutzte Rechner bedeutet.
- Testsuite auf 138 Prüfungen erweitert; der Test-Ambient lädt WinForms, weil auf einem sauberen CI-Runner sonst die Wiederholungslogik der Anmeldung fehlschlug.

## 0.15.4 – Versionsbereinigung funktioniert wieder, Tests, Bestätigungsdialog, Spaltenbeschriftung

**Berechtigungen und Konto**

- **Klar dokumentiert, welche Graph-Berechtigungen wofür gebraucht werden** – je Berechtigung mit Zweck und Angabe, ob lesend oder schreibend. Im README als eigener Abschnitt „Konto und Berechtigungen", in der Anwendung unter Hilfe → „Benötigte Berechtigungen".
- **Empfehlung gegen den globalen Administrator.** Für den Betrieb genügt die eingebaute Intune-Rolle *Application Manager*, für reine Auswertung *Read Only Operator*; empfohlen wird ein dediziertes Administrationskonto mit MFA, dessen Wirkungsbereich sich zusätzlich über Bereichs-Tags einschränken lässt. Dass die einmalige Zustimmung ein Entra-ID-Konto mit entsprechender Berechtigung braucht, der laufende Betrieb aber nicht, steht jetzt ebenfalls dabei.

**Aus einer externen Code-Durchsicht**

- **Der Versionsvergleich widersprach sich selbst.** `Test-IsNewerVersion` nutzte zuerst den `[version]`-Typ, der eine fehlende Komponente als `-1` zählt: `1.2.0` galt damit als neuer als `1.2`, während `Test-VersionsEquivalent` dieselbe Paarung als gleich betrachtete. Beide Funktionen werden im Suchlauf und in der Selbstaktualisierung nebeneinander benutzt; eine App konnte als update-bedürftig gemeldet werden, obwohl sie es nicht war. Es gibt jetzt nur noch einen Vergleichsweg.
- **Der Schutz vor Systemordnern war von der Schreibweise abhängig.** In derselben Zeile trafen zwei Vergleichsarten aufeinander: PowerShells `-eq` ignoriert Groß-/Kleinschreibung, .NETs `String.StartsWith` nicht. `c:\program files\…` passierte die Sperre, `C:\Program Files\…` nicht. Die Prüfung sitzt jetzt in einer eigenen, testbaren Funktion und vergleicht durchgängig ohne Rücksicht auf Schreibweise.
- **Die Gruppensuche kodierte den Suchbegriff nicht für die URL.** Ein Gruppenname mit `&`, `#` oder `%` zerlegte die Abfrage oder hängte eigene Parameter an.
- **Das veröffentlichte Release wurde nie geprüft.** Die Static Checks liefen vor dem Einsetzen der Repository-Kennung, danach nur noch ein Parser-Lauf – das Artefakt, das Anwender herunterladen, hatte die Prüfungen also nie durchlaufen. Sie laufen jetzt danach.
- Eine folgenlose, aber irreführende Verzweigung in der Erkennungsregel entfernt (beide Zweige lieferten `exists`).

**Tests**

- **Erste echte Testsuite: 101 Pester-Prüfungen** unter `tests/Unit`, ausgeführt in einem eigenen CI-Auftrag. Abgedeckt sind die Funktionen mit echter Logik: Versionsvergleich, Versionsgruppierung, Installationssonde samt Report-Rückfall, Leistungsnachweis, Sicherungsrotation, Anmelde-Wiederholung, Aufräum-Optionen und Systemordner-Schutz. Jeder in dieser Runde behobene Fehler hat eine Regressionsprüfung bekommen.

- **Der Leistungsnachweis erfasst jetzt auch alles andere.** Bisher zählte er ausschließlich Updates, weshalb eine Sitzung, die acht alte Versionen aufgeräumt hat, „noch keine Apps aktualisiert" meldete. Aufgezeichnet werden nun zusätzlich entfernte alte Versionen, gelöschte abgelöste Apps und Zuweisungsänderungen, jeweils in eigenen Abschnitten mit passender Zusammenfassung. Einträge aus älteren Fassungen ohne Typ werden weiterhin als Update gelesen.
- **Die Anzahl zu behaltender Versionen ist einstellbar.** Statt fest 2 lässt sich in den Einstellungen ein Wert zwischen 1 und 20 wählen. Der Knopf in der Karte der abgelösten Apps, sein Tooltip und die Checkbox übernehmen die Zahl sofort.
- **Der Update-Suchlauf nach dem Login ist jetzt standardmäßig aus.** Die Option „Beim Login nach Updates suchen" gab es schon, war aber voreingestellt aktiv; in großen Tenants durchsucht sie nach jeder Anmeldung jede App und lässt die Oberfläche lange laden. Wer den Automatismus will, aktiviert die Checkbox in den Einstellungen. Bereits gespeicherte Einstellungen behalten ihren bisherigen Wert.
- **Feature-Anfrage per E-Mail entfernt.** Der Menüpunkt, der Dialog und die darin hinterlegten Standard-Empfänger sind vollständig aus der Anwendung verschwunden. Es sind keine Kontaktadressen mehr im Programm enthalten.
- **Deutlicher Hinweis, wann die Versionsbereinigung eine Version stehen lässt.** Tooltip und Info-Text erklären jetzt, dass „neueste N behalten" eine Kandidatenliste ist: Entfernt wird erst, wenn Intune null Zuweisungen **und** null installierte Geräte meldet. Eine noch installierte ältere Version bleibt bewusst erhalten – auch wenn dadurch mehr als N Versionen übrig bleiben – und wird von einem späteren Lauf abgeräumt, sobald die Geräte gewechselt haben.

- **Der Installationszustand wird notfalls aus der Report-API gelesen.** In Tenants, die auf *jede* App-bezogene Navigationseigenschaft mit HTTP 400 antworten – `deviceStatuses`, `userStatuses` und `installSummary`, auf beta wie v1.0, mit und ohne Typ-Cast – blieb der Zustand dauerhaft unbekannt. Da unbekannt jede Löschung blockiert, meldete die Versionsbereinigung Lauf für Lauf „0 entfernt, N behalten" und konnte ihre Aufgabe nie erfüllen. Als dritte Quelle wird jetzt `getAppStatusOverviewReport` abgefragt – dieselbe Quelle, aus der auch das Intune-Portal den App-Installationsstatus zieht. Die Spalte wird über ihren Namen gesucht, und jede unerwartete Antwort ergibt weiterhin „unbekannt", blockiert also die Löschung, statt sie zu erlauben.
- Derselbe Fehler wirkte über die Bereinigung hinaus: Konsolidierungen räumten alte Objekte nie ab, und Updates unzugewiesener Apps wurden ohne Ablöse bereitgestellt.
- **Auch „Value cannot be null" wird bei der Anmeldung wiederholt.** Derselbe Wettlauf im Modul wie „Collection was modified", nur mit anderem Wortlaut; er wurde bisher als harter Fehler gemeldet und zwang zu einem manuellen zweiten Anmeldeversuch.
- Die Abschlusszeile der Versionsbereinigung sprach von „kept back (errors)", obwohl eine korrekt geschützte Version kein Fehler ist.

- **Die Versionsbereinigung zählte App-Objekte doppelt.** Sie liest die aktive und die abgelöste Bestandsliste und hängte beide ohne Abgleich aneinander. Liefert das Modul dieselbe App in beiden Listen – was vorkommt, kurz nachdem eine Ablöse angelegt wurde –, belegte dieser Doppeleintrag einen der „neueste N behalten"-Plätze und schob eine Version, die hätte bleiben sollen, in die Löschliste. Es wird jetzt über die GraphId entdoppelt.
- **Übersprungene Objekte werden einzeln protokolliert.** Wird ein App-Objekt aus dem aktiven Suchlauf ausgeschlossen, weil dieselbe GraphId bereits als abgelöst gemeldet wird, stand bisher nur eine Anzahl im Protokoll. Jetzt steht dort Name, Version und GraphId – genau diese Objekte sind für den Suchlauf auch als *wiederverwendbares Ziel* unsichtbar.

- **Der Bestätigungsdialog beschreibt jetzt die tatsächliche Konfiguration.** Bisher gab es zwei feste Absätze, gesteuert von einer einzigen Option. Ob die Zuweisungen auf die neue Version umziehen und ob danach ältere Versionen entfernt werden – beides verändert den Tenant – stand nicht darin. Der Hinweis wird nun aus allen drei wirksamen Einstellungen zusammengesetzt, inklusive der eingestellten Anzahl zu behaltender Versionen.
- **Spalte „Ziel in Intune" heißt jetzt „Zielversion".** „Ziel" wurde wiederholt als Zuweisungsziel gelesen, also als Aussage darüber, ob eine App zugewiesen ist. Die Spalte beantwortet aber, ob die Version, auf die aktualisiert wird, in Intune bereits als App existiert. Die Werte „neu anzulegen" und „bereits vorhanden" bleiben unverändert.

## 0.15.3 – Die Selbstaktualisierung hat noch nie funktioniert

- **Kein Speichern-Dialog mehr beim Aktualisieren.** Konnte der Pfad des laufenden Skripts nicht ermittelt werden, öffnete sich ein „Speichern unter"-Dialog – der die Frage an den Benutzer weiterreichte und obendrein nur eine Kopie anlegte, statt das laufende Skript zu ersetzen. Nach der Bestätigung läuft der Austausch jetzt ohne weitere Rückfrage durch; ist der Pfad unbekannt, wird das klar gemeldet.
- **Sicherungen werden rotiert.** Die Sicherung heißt jetzt `<Skript>.<Zeitstempel>.backup`, sodass sich auf eine bekannte Version zurückgehen lässt. Die beiden jüngsten bleiben erhalten, ältere werden beim nächsten Update entfernt – einschließlich der einzelnen `.backup`-Datei früherer Versionen. Schlägt das Löschen fehl, wird das protokolliert und das Update trotzdem abgeschlossen.

- **Die Update-Prüfung meldete immer „aktuell", unabhängig davon, was auf GitHub liegt.** Sie lief über einen `BackgroundWorker`; ein solcher Thread hat keinen PowerShell-Runspace, weshalb der übergebene Scriptblock überhaupt nicht ausgeführt wurde – nicht einmal sein eigenes `try/catch`. Der Aufrufer bekam `$null` zurück, und der Code deutete das als „kein Update verfügbar". Erkennbar war es nur an der Anzeige `GitHub version: vunknown`. Die Prüfung läuft jetzt wie jede andere lange Operation auf dem UI-Thread.
- **Ein Ergebnis ohne Version gilt nicht mehr als „aktuell".** Fehlt die Versionsangabe, wird das als fehlgeschlagene Prüfung gemeldet. Vorher war eine dauerhaft kaputte Prüfung nicht von „du bist auf dem neuesten Stand" zu unterscheiden.
- **`Invoke-AsyncOperation` entfernt.** Der Helfer war die Ursache und hatte nach der Umstellung keinen Aufrufer mehr; 25 Kommentare, die eine Umstellung *auf* ihn empfahlen, wurden korrigiert. Dieselbe Ursache hatte seinerzeit die leeren Dashboard-Kacheln.

## 0.15.2 – Laufzeit des Update-Laufs, blockierender Hintergrund-Dialog, Anmeldung

- **Kein automatischer Suchlauf mehr nach einem Update.** Nach jedem Durchlauf wurde der Tenant komplett neu eingelesen und für jedes Paket erneut WinGet befragt – rund 10–15 Sekunden ungefragtes Warten. Die Zeilen erfolgreich aktualisierter Apps verschwinden weiterhin sofort aus der Liste; eine erneute Prüfung startet nur noch „Updates suchen“.
- **Blockierender Dialog hinter dem Fenster behoben.** Die automatische Versionsbereinigung stieß am Ende eine Ansichtsaktualisierung an, während der Fortschrittsbalken noch lief. Das lief in die „Es läuft bereits ein Vorgang“-Sperre und öffnete einen modalen Dialog – meist hinter dem Hauptfenster, wo er den Durchlauf anhielt, bis ihn jemand fand (in einem Lauf 58 Sekunden).
- **Versionsbereinigung funktioniert in Tenants, deren `deviceStatuses` mit HTTP 400 antwortet.** Dort blieb der Installationszustand dauerhaft „unbekannt“, und weil unbekannt jede Löschung blockiert, wurde nie eine alte Version entfernt – „0 entfernt, N behalten“, Lauf für Lauf. Als Rückfallebene wird jetzt `installSummary` abgefragt; erst wenn auch das scheitert, bleibt der Zustand unbekannt.
- **Doppelte Abfragen entfernt.** Bei der Konsolidierung auf ein vorhandenes Ziel wurden Zuweisungs- und Installationszustand zweimal hintereinander abgefragt, obwohl dazwischen nichts verändert wurde.
- **Anmeldung übersteht kurzlebige Fehler.** Die erste Intune-Abfrage nach dem Login scheiterte gelegentlich mit „Collection was modified“ – ein Wettlauf im Modul, kein Anmeldeproblem – und wurde als Authentifizierungsfehler gemeldet. Solche Fehler (auch Drosselung, 503/504) werden jetzt bis zu dreimal wiederholt. Eine fehlende Berechtigung scheitert weiterhin sofort.
- **Zeiten im Protokoll getrennt.** Upload und das anschließende Auflösen der neuen App werden einzeln gemessen, damit ein langsamer Lauf zuzuordnen ist.

## 0.15.1 – Schutz vor unbeabsichtigtem Löschen der Vorgängerversion

- **Vorgänger wird nur noch gelöscht, wenn die neue Version nachweislich zugewiesen ist.** Bisher genügte als Nachweis, dass die alte App keine Zuweisung mehr hat – das trifft auch zu, wenn die Übergabe stillschweigend fehlgeschlagen ist. Jetzt wird die neue App direkt abgefragt (mit Wiederholungen, weil Intune kurz nach dem Zuweisen verzögert antwortet); ohne bestätigte Zuweisung bleibt die alte Version stehen. Gilt für die Ablöse nach einem Upload ebenso wie für die Konsolidierung auf ein bereits vorhandenes Ziel.
- **Die beiden Aufräum-Optionen schließen sich jetzt technisch aus.** „Auch zugewiesene alte App-Version nach erfolgreichem Update entfernen“ und „nur die neuesten N Versionen behalten“ konnten gleichzeitig aktiv sein; die erste läuft im Update, die zweite erst danach – dadurch wurde der Vorgänger gelöscht, obwohl die zweite Option ihn behalten sollte. Das Anhaken der einen schaltet die andere ab, und eine bereits gespeicherte Kombination wird beim Start zugunsten der nicht-löschenden Option aufgelöst und einmalig erklärt.

## 0.15.0 – Erste öffentliche Beta

Erste als Beta gekennzeichnete Veröffentlichung. Funktionen und Bedienabläufe können sich zwischen Versionen noch ändern; für den produktiven Einsatz ist die Anwendung nicht freigegeben.

**Funktionsumfang**

- **WinGet-Apps** suchen, Version wählen, lokal als Intune-Win32-Paket erstellen und nach Bestätigung bereitstellen – einschließlich Zielgruppe, Intent, Filtern, Benachrichtigungen und Fristen.
- **Microsoft-Store-Apps** über den Store-Katalog suchen, aus den Treffern auswählen und bereitstellen. Bereits im Tenant vorhandene Store-Apps werden angezeigt, um Doppelbereitstellungen zu vermeiden.
- **Updates** vorhandener Intune-Apps gegen WinGet und den WinTuner-Index prüfen. Die Ergebnisliste trennt erforderliche Uploads von reiner Nacharbeit und weist Konflikte wie doppelt vorhandene Versionen aus, statt sie stillschweigend auszulassen.
- **Alle Tenant-Apps** einsehen – jedes App-Objekt jeden Typs, auch solche, die diese Oberfläche nicht paketiert. Zuweisungen werden im Klartext angezeigt und lassen sich verwalten: Gruppen und Ausschlüsse hinzufügen oder entfernen, Absicht festlegen, Bereitstellungseinstellungen ändern. Gruppen können optional über ihren Namen gesucht werden.
- **Eigene Installer** paketieren: beliebige EXE- oder MSI-Installer zu einem `.intunewin`-Paket verarbeiten, auch Software, die es in WinGet nicht gibt.
- **Erkennungsregel ermitteln**: MSI-Produktcode direkt auslesen, oder die Uninstall-Registrierung vor und nach einer Installation vergleichen und daraus eine fertige Intune-Erkennungsregel ableiten. Silent-Schalter lassen sich in der Windows Sandbox testen, ohne den eigenen Rechner zu verändern.
- **Inhalt vorhandener Apps ersetzen**, statt eine zweite App anzulegen und die alte abzulösen. App-ID, Zuweisungen und Historie bleiben erhalten.
- **Erkannte Software** aus dem Intune-Inventar auswerten und WinGet-Paketen zuordnen.
- **Alte App-Versionen bereinigen** – nur nach erneuter Prüfung von Zuweisungen und gemeldeten Installationen; risikoreiche Optionen sind standardmäßig aus.
- **Lokale Pakete und Favoriten** pflegen, optional automatisch beim Start prüfen.
- Deutsche und englische Oberfläche, mehrere Darstellungsmodi, wöchentliche Aktivitätsprotokolle.

**Sicherheit**

- Pakete werden unter `%LOCALAPPDATA%\WinTunerGUI\Packages` erstellt. Ein gemeinsam beschreibbarer Ablageort ist bewusst nicht voreingestellt, damit ein fertiges Paket zwischen Erstellung und Upload nicht von anderen Benutzern des Rechners verändert werden kann.
- Jede Änderung an Intune erfordert eine ausdrückliche Bestätigung; das Ändern von Zuweisungen zeigt vorher die vollständige Liste, die geschrieben wird.
- Keine Kennwörter, Token oder Zugangsdaten im Skript oder in der Einstellungsdatei. Die Anmeldung läuft über Microsoft Entra ID.
- Die Selbstaktualisierung akzeptiert nur Releases mit passendem Asset, SHA-256-Prüfsumme und plausibler Versionsnummer und legt vorher eine Sicherung an.
